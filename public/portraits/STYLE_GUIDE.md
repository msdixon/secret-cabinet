# Portrait Style Guide

Source of truth for generating member portraits (#116, #27). Every generation run — the initial batch and any member added afterward — should be checked against this file first. If a run has to deviate to get a usable result, record it in the Changelog below so the next run doesn't silently drift further.

## Status

AI-generated, proof-of-concept register for this stage of the project. Not commissioned or licensed illustration. See [MANIFEST.md](../../docs/MANIFEST.md) for the standing disclosure note.

## Baseline register (from #27)

- **Tone:** warm, etching-adjacent — not photorealistic, not cartoon/flat-vector.
- **Palette:** limited, warm-toned (sepia/candlelit register), consistent across the set — no member should read as lit or colored differently from the rest of the roster.
- **Composition:** head-and-shoulders, period-appropriate dress, plain/dark background (no scene elements — scene context is the 3D salon's job, not the portrait's, per #26/#28).
- **Line quality:** visible linework/texture (etching, engraving, ink-wash adjacent) rather than smooth airbrushed rendering.

## Known-vs-unknown likeness

- **Photographed figures** (e.g. Crowley, Yeats): generations should aim for recognizable likeness where reference material exists.
- **Pre-photographic or iconography-bound figures** (e.g. Llull, Teresa of Ávila, Ibn Arabi): there is no photographic ground truth — treat these as period-appropriate character studies consistent with the set's register, not likeness reproductions. For figures with an existing devotional/iconographic tradition, favor a secular character-study framing over reproducing religious iconography.

## Downstream use and format

**Decided 2026-07-31** (see [#26](https://github.com/msdixon/secret-cabinet/issues/26)): the existing batch-1 crop (`public/portraits/<id>.png`, portrait-oriented rectangle) is used as-is for the 3D avatar's Phase 2 billboard cards — no new crop or generation needed. Billboards (camera-facing textured planes) are the deliberate MVP, not a placeholder for something else already planned; a more sculptural avatar representation is the intended eventual direction, and full 3D character models are explicitly a long-horizon idea, not a near-term target — so don't assume billboards need re-doing once the "next" avatar phase starts. If a genuinely different rendering approach (true 3D geometry) is ever taken up, that would need its own crop/format decision at that time.

## Process

1. Batch 1: generate the full current roster (see `prompts/members/roster.json`) in one run to establish the baseline and catch consistency problems before they're spread across separate sessions.
2. Human-validate batch 1 against this guide before treating the style as locked.
3. Reconvene to confirm the baseline (or amend this guide) before generating any member added after batch 1.
4. For each member added to the roster afterward, generate one portrait against this guide, at the same time the member is added. **As of [#259](https://github.com/msdixon/secret-cabinet/issues/259):** the prompt-drafting half of this step is automated — the in-app "Add Member" flow (`POST /api/members`, `src/routes/member.js`) drafts a style-guide-compliant prompt entry against this file at member-creation time and appends it to [PENDING-PROMPTS.md](PENDING-PROMPTS.md). Generating the image from that prompt (currently via Nano Banana), resizing to 512px on the long edge, placing it at `public/portraits/<id>.png`, and human-validating the result stay manual, same as every prior wave — only the hand-drafting of the prompt text itself is automated. Deleting the entry from PENDING-PROMPTS.md and logging the placement below is still the last manual step.

## Changelog

- 2026-08-25 — [#259](https://github.com/msdixon/secret-cabinet/issues/259) scoped and given a first cut: the in-app "Add Member" flow now also drafts a portrait-generation prompt (via the same Claude client already used for character-file drafting) against this file, using [WAVE-4-PROMPTS.md](WAVE-4-PROMPTS.md) as the format exemplar, and appends it to the new [PENDING-PROMPTS.md](PENDING-PROMPTS.md). Full image-generation automation (calling an image-gen API directly) was considered and deliberately deferred — no such API is connected in this project's Claude Code environment, it would need a cost/provider decision from Rachel, and this project's own process treats human validation of each portrait as load-bearing, not a step to automate away. Process step 4 above reflects the new split between automated (prompt drafting) and manual (generation, resize, placement, validation).
- 2026-08-12 — Wave 4 backfill complete ([#258](https://github.com/msdixon/secret-cabinet/issues/258)): Meister Eckhart, Jakob Böhme, Emanuel Swedenborg, Paracelsus, and G.I. Gurdjieff generated against [WAVE-4-PROMPTS.md](WAVE-4-PROMPTS.md) via Nano Banana and placed in `public/portraits/` — all 38 roster members now have a portrait. These were the 5 members added by the Wave 4 roster expansion ([PR #249](https://github.com/msdixon/secret-cabinet/pull/249), 2026-08-10) that shipped without portraits, closing the gap Process step 4 was supposed to cover in the same PR. Resized from source (1696-1792px / 2400-2528px) to 512px on the long edge, matching the rest of the set; full-res sources kept locally (`~/Downloads`), not committed. No consistency problems found against the baseline register on visual check.
- 2026-07-31 — Batch 1 complete: Julian, Hildegard, and Porete generated against the v4/v3 prompts and placed in `public/portraits/` (resized to 512px on the long edge, matching the rest of the set) — all 33 roster members now have a portrait. The structural differentiation held: Julian reads as a specific, elderly (not ~30) figure glimpsed through the anchorhold window with candlelight behind her; Hildegard's severity and staff of office read as clearly authoritative and visibly older than Julian; Porete's youth, loose lay dress, and displayed book (with legible title) read as distinctly different from both. No id-label or other placement issues found on any of the three. Full-res sources kept locally, not committed, same as the rest of the batch. Process step 1 (generate full roster) is now done; step 2 (human-validate against this guide) — Rachel has validated the 30 placed via PR #145, these 3 pending her review of this follow-up PR.
- 2026-07-31 — Julian's v3 prompt corrected to v4 before ever being run: Rachel caught that "deliberate anonymity" had been scoped too broadly and was suppressing the age/habit specificity that made Hildegard and Porete's revisions actually work (the generator had been defaulting her to ~30, generic dress). v4 keeps the window-aperture framing but explicitly separates "no known face/name" (stays unspecific) from everything else about her (age — elderly, 50s-60s, tied to the Long Text's twenty-years-later maturity — dress, era, region — all should render with full period confidence). Still not yet generated.
- 2026-07-30 — Julian/Hildegard/Porete prompts revised again (v3, in BATCH-1-PROMPTS.md) — v2's single added detail (quill, book, window suggestion) was likely too subtle against three prompts that otherwise shared the same shape. v3 differentiates structurally instead: different ages (Porete 30s-40s, Hildegard 70s-80s), different emotional register (defiant vs. commanding vs. withdrawn), different garment tone (undyed lay wool vs. severe black habit vs. barely-visible in shadow), and for Julian specifically, a deliberate break from the set's head-and-shoulders/plain-background composition rule — framed through the anchorhold's stone window aperture instead, flagged explicitly as a rule deviation per this doc's own instruction, pending Rachel's sign-off before running. Not yet generated or placed — batch 1 otherwise validated and merged (30/33) via [PR #145](https://github.com/msdixon/secret-cabinet/pull/145).
- 2026-07-30 — 30 of 33 batch 1 portraits placed in `public/portraits/`, resized from source (~1700-2050px) to 512px on the long edge (277MB → 10MB total — full-res sources are not committed, kept locally in `~/Downloads` as the archival copies pending #117's 3D-texture format decision). Two mechanical fixes applied before placement, not caught until inspecting every file individually (an automated dark-pixel heuristic proved unreliable — both false positives and negatives against the set's varying image dimensions and border styles): 5 files (crowley, waite, pixie/Coleman-Smith, yeats, blavatsky) had an identification label baked into the bottom of the image from whatever tool split them out of the review grid — cropped out at the source resolution before the resize. Levi and Teresa each had extra duplicate generations (`levi1.png`, `teresa2.png`, `teresa3.png`) — base-named files used as canonical per direction, duplicates left unplaced. Held back, not yet placed: Julian, Hildegard, Porete (see prior entry — still converged even after the v2 prompt revisions, per direct visual comparison; a William+Catherine Blake two-up image was also generated as an extra alongside working individual portraits for both and was discarded, not placed).
- 2026-07-30 — First generation pass run against the batch 1 prompts (external tool). Most of the set reads well and distinctly. One real issue found: Julian of Norwich, Hildegard of Bingen, and Marguerite Porete converged on near-identical images — their v1 prompts shared the same shape ("plain habit/veil, medieval woman, no likeness reference") with nothing structurally distinct for the generator to anchor on. Revised in [BATCH-1-PROMPTS.md](BATCH-1-PROMPTS.md) (marked v2) with one concrete, secular, non-devotional distinguishing detail per figure — Porete: informal lay veil with visible hair + a held book; Hildegard: severe wimple, elderly bearing, quill and manuscript corner; Julian: shadowed/downcast face, faint anchorhold-window detail in the background. Also surfaced: a two-up William Blake + Catherine Blake image generated together rather than as separate portraits — needs a re-run, since the two need independent `william-blake.png`/`catherine-blake.png` files. Batch still mid-validation; not yet placed into `public/portraits/`.
- 2026-07-30 — Batch 1 prompts drafted for the full 33-member roster: [BATCH-1-PROMPTS.md](BATCH-1-PROMPTS.md). No image-generation tool is connected in this project's Claude Code environment, so prompts are written for external generation (Midjourney/DALL-E/etc.) rather than run in-repo. Actual images not yet generated — pending Rachel running the prompts and dropping results into `public/portraits/<id>.png`.
- 2026-07-27 — Guide established ahead of batch 1. No baseline images generated yet.
