[#378](https://github.com/msdixon/secret-cabinet/issues/378) scope the four session read routes by `published` — `GET /api/sessions` currently returns every session with journal-entry previews and full-text search and no filter. Prerequisite for [#379](https://github.com/msdixon/secret-cabinet/issues/379); ships as a behavioural no-op while the passphrase gate is still in front of everything
