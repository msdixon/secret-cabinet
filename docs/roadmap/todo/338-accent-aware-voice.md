[#338](https://github.com/msdixon/secret-cabinet/issues/338) Regional-accent-aware voice assignment, the remaining half after the demeanor half shipped ([PR #340](https://github.com/msdixon/secret-cabinet/pull/340)). Needs a live check against this account's actual ElevenLabs voice library (premade + the larger searchable library, not just the 21-voice `FALLBACK_VOICE_IDS` fallback pool) before picking a mechanism — not possible without a configured `ELEVENLABS_API_KEY`. Separately, unclear whether Web Speech's `SpeechSynthesisVoice.lang` (locale, not true accent) gives any workable parity at all.
