[#379](https://github.com/msdixon/secret-cabinet/issues/379) shipped: an unauthenticated visitor now lands on the room itself, with published meetings reachable via `?session=<id>`, while convening stays behind the passphrase. `src/auth.js`'s `createRequireAuth` grows from three ad-hoc path bypasses into a generalized public/gated split — any non-API GET is public (the app shell), plus a fixed method+path allowlist for read-only API routes including the four `#378`-scoped session routes, now reachable in production for the first time. The control-rail renders `inert` under a sign-in overlay rather than hidden, per Principles 4 and 6. Verified live as both an unauthenticated and authenticated visitor, including a published/unpublished session pair confirming the exposure boundary holds. 924 tests. [PR #390](https://github.com/msdixon/secret-cabinet/pull/390)
