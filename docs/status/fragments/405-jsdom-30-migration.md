Bumped `jsdom` 29.1.1 → 30.0.1 (dev-only, `test/*.test.js`), the half of #251 split out as [#405](https://github.com/msdixon/secret-cabinet/issues/405) after Express 4→5 shipped separately under #136. Full suite (965 tests) passes unchanged; the suite has no `getComputedStyle`/CSS-cascade-adjacent assertions at all, so the risk category #251 flagged didn't actually apply here. One finding worth knowing: jsdom 30 declares `engines.node: "^22.22.2 || ^24.15.0 || >=26.0.0"`, which excludes Node 25.x (an odd, non-LTS release) — advisory `EBADENGINE` warning only, doesn't block install or tests, and CI's Node 22 (latest 22.23.2) is unaffected.
