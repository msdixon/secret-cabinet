Fixed room speech cards opening scrolled to the tail of a long passage, burying its opening lines behind the sticky `.speaker-name` header — `witness.js`'s `scrollCardToLatest()` was jumping to `scrollHeight` unconditionally, right for #287's stacked-beats/actively-typing cases but wrong for a beat that lands complete in one shot (replay's path). Added a `scrollCardToEntryStart()` used only for freshly-appended entries; live-typing tail-follow and #287's behavior verified unaffected. [#476](https://github.com/msdixon/secret-cabinet/issues/476), [PR #485](https://github.com/msdixon/secret-cabinet/pull/485).
