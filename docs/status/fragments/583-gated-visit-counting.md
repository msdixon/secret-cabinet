[#583](https://github.com/msdixon/secret-cabinet/issues/583): extended [#422](https://github.com/msdixon/secret-cabinet/pull/428)'s visit counting to the gated side — convene/cast/round/interject calls from a session authenticated past `applyConveneGate()`, same count/log-only approach, surfaced as a new section in the existing `GET /api/admin/visits` report. Excludes local dev, where every request is authenticated by default and would otherwise swamp the count. [PR #588](https://github.com/msdixon/secret-cabinet/pull/588).
