[PR #524](https://github.com/msdixon/secret-cabinet/pull/524) fixed `speakViaElevenLabs()` setting `audio.playbackRate` before `audio.src` — the media-element load algorithm resets `playbackRate` to `defaultPlaybackRate` on `.src` assignment, so ElevenLabs audio always played at native 1x regardless of the Playback Speed control. Verified live against the real ElevenLabs backend, and resolves mechanism #2 from [#505](https://github.com/msdixon/secret-cabinet/issues/505)'s diagnosis; #505 stays open for its separate per-beat text-before-audio latency finding. Fixes [#518](https://github.com/msdixon/secret-cabinet/issues/518).
