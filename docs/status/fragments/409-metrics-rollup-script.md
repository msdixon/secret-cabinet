Added `scripts/rollup-metrics.js` (`npm run metrics-rollup`), a read-only aggregation of every session's `generationMetrics` into total/by-phase/by-date cost and volume, gitignored `METRICS-ROLLUP.md`. Flags when a scanned session likely predates [#225](https://github.com/msdixon/secret-cabinet/issues/225) or [#190](https://github.com/msdixon/secret-cabinet/issues/190)'s field additions, per `docs/MODEL-REVIEW.md` Step 1's own caveat, and notes it can't break totals out by model since no metric entry records which model generated it. [#409](https://github.com/msdixon/secret-cabinet/issues/409).
