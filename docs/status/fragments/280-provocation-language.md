#280 (direct-question entry) shipped as a pure copy/prompt change rather than a new entry-point mode — the pipeline already treated the entry field as arbitrary text and grounding was already independent of input type, so every place the app said "document" (UI, casting prompt, passage preamble, arc notes) now says "provocation" instead, a word that covers a bare question equally. Live-verified against a real question through the actual UI before merging. [PR #328](https://github.com/msdixon/secret-cabinet/pull/328)
