Bumped `dotenv` 17.4.2→18.0.1 (#599); audited `server.js` and 23 scripts and none use the `dotenv/config` preload or the removed `.env.vault` support, so the upgrade needed no code changes. The default "injected env" confirmation now prints to stderr instead of stdout, which only shows up in the scripts that don't pass `quiet: true` (`server.js` does). [#599](https://github.com/msdixon/secret-cabinet/issues/599)
