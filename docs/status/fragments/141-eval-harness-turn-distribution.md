Shipped the first-cut evaluation harness scoped in the 2026-08-20 `/cabinet-review` comment on [#141](https://github.com/msdixon/secret-cabinet/issues/141): `scripts/eval-harness.js` (`npm run eval-harness`) reports per-meeting turn distribution and zero-turn shares from real sessions, beats-per-passage and `endedBy` breakdown, and a replayable seeded-RNG simulation of the real `pickNextSpeaker`/`isPoolExhausted` against current `tuning.js` constants — generalizing the inline harness #352/#353 built in `test/pipeline.test.js` into something re-runnable after a tuning change, with no API calls spent. Verified against current post-#352/#353 behavior (~0% zero-turn share, reconsult rate inside the 5%-95% band), not the issue's own pre-fix numbers. [PR #419](https://github.com/msdixon/secret-cabinet/pull/419).
