First cut of [#34](https://github.com/msdixon/secret-cabinet/issues/34), "the document as object": a static open-book model on the table (hidden until a provocation is set), click-to-inspect camera framing plus a DOM reading panel for the text, and a page-glow tied to `setSpeaking()` standing in for "members gesture toward it" (no character rig exists to animate). Deliberately scoped like #357/#304's own first passes — per-passage highlighting synced to #355's citation capture is left for a later pass, not built here. [PR #455](https://github.com/msdixon/secret-cabinet/pull/455).
