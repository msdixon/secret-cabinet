A member can now genuinely decline a turn — `isPassTurn` detects the room's own action-only idiom as a deliberate pass, recorded as a distinct `passed: true` beat (never conflated with a failed call) and fed back into scheduling via partial ledger credit and a budget floor, per [#362](https://github.com/msdixon/secret-cabinet/issues/362). Also fixes a client-side gap the issue surfaced: `addSpeech` rendered any all-action turn with no speaker attribution, which would have made a deliberate pass indistinguishable from anonymous stage business ([#394](https://github.com/msdixon/secret-cabinet/pull/394)).
