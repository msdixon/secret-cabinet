Shipped a first cut of [#196](https://github.com/msdixon/secret-cabinet/issues/196)'s splinter mechanic — two members trade a private aside while the main passage continues, generated on its own private context (not the shared `roundSoFar`) and reusing the existing #203 interrupt-intent signal as its trigger, then folded into the record as a diegetically bracketed block so every existing surface renders it correctly with no client change. Re-checked the #194 blocker first: its storage half was already resolved by #194's beat-level persistence, so this closes the remaining generation-side gap. Visual grammar (#184/#257's stage) and a director-initiated trigger are named as deliberate follow-ups, not gaps. [PR #456](https://github.com/msdixon/secret-cabinet/pull/456).
