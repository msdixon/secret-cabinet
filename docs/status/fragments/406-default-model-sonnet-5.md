Switched the `MODEL` default in `server.js` to `claude-sonnet-5`, the follow-through on [MODEL-REVIEW.md](docs/MODEL-REVIEW.md)'s 2026-08-08 recommendation ([#216](https://github.com/msdixon/secret-cabinet/issues/216)). Added `thinking: {type: "disabled"}` to the two calls flagged as too tight for adaptive thinking's default budget (`callDirector`'s `select_speakers`, `callDispositionUpdate`). [#406](https://github.com/msdixon/secret-cabinet/issues/406) — voice-fidelity spot-check still outstanding, logged in the review table.
