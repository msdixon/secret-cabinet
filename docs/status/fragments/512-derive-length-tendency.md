[#512](https://github.com/msdixon/secret-cabinet/issues/512) phase 1 — `LENGTH_TENDENCY_OVERRIDES` in `src/tuning.js` is now derived from each member's own #187 voice exemplar (average words/sentence, bucketed by quartile) via `scripts/derive-length-tendency.js`, replacing a hand-tagged list that only ever covered Crowley and Yeats. Still plain static config, computed once and checked in — the adaptive, observed-turn-length version is deliberately deferred as [#542](https://github.com/msdixon/secret-cabinet/issues/542).
