Pre-convene flow: dismissible preamble banner + a numbered 3-step stepper (Bring a provocation → Choose who's in the room → Convene) with live checkmark/active/pending states, reconciled against #358's shipped room-above-controls layout rather than ported from the original mockup. The mockup drew a full-screen wizard header for real estate that no longer exists above the fold; instead the stepper is a compact wayfinding strip placed above `.control-rail` (not inside it, so it stays legible through the `#379` sign-in gate rather than dimming with the controls), with each step reading its own completion state independently since bringing a provocation and casting the room can happen in either order. See [#342](https://github.com/msdixon/secret-cabinet/issues/342).
