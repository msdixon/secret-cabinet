[#458](https://github.com/msdixon/secret-cabinet/issues/458) added the second splinter trigger #196/PR #456 deferred: the director can now propose a `splinterPair` directly on its existing `select_speakers` call (the `windingDown`/`lullNote` pattern), opening a private aside — "Crowley leans toward Coleman-Smith" — with no live #203 interrupt-intent signal needed. It shares the reactive trigger's `MAX_SPLINTERS_PER_PASSAGE` cap and budget-headroom gate, with no rng roll of its own: the director's own prompt-level restraint is the gate instead. [PR #501](https://github.com/msdixon/secret-cabinet/pull/501)
