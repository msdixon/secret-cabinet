[#338](https://github.com/msdixon/secret-cabinet/issues/338) demeanor half shipped: a hand-set `voiceDemeanor` (grounded/stately/intense) per roster member layers on top of #333's gender filter in both TTS backends — `assignVoiceId` narrows by demeanor after gender (ElevenLabs), and `voice.js` biases the deterministic per-member pitch/rate since browsers expose no per-voice demeanor signal to filter against (Web Speech). All 38 members' `voiceId` re-derived so the assignment is actually live. Accent stays open on #338 — needs a live ElevenLabs voice-library check this environment couldn't run. [PR #340](https://github.com/msdixon/secret-cabinet/pull/340)
