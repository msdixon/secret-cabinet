[PR #532](https://github.com/msdixon/secret-cabinet/pull/532) fixed room speech cards fading before their audio finished: `scheduleCardFade` now waits on the same `Voice.speak()` promise `#336`'s pacing fix already awaits, not just `witnessReadingTime`'s WPM estimate, so a card is held until whichever is longer — the WPM estimate or real audio completion — before its grace period starts. Live-verified with real ElevenLabs audio, not just unit tests. Closes [#529](https://github.com/msdixon/secret-cabinet/issues/529).
