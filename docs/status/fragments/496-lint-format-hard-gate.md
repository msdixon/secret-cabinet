[PR #544](https://github.com/msdixon/secret-cabinet/pull/544) closes [#496](https://github.com/msdixon/secret-cabinet/issues/496) for good: the report-only lint/format backlog had regrown and been manually reset three times in a week because `continue-on-error: true` made the CI step report success regardless of its actual result, so a PR reintroducing drift still showed green. Fixed the 2 lint errors and 2 format-drifted files on `main`, confirmed clean against a fresh CI run, then dropped `continue-on-error` from both steps. Also found `main` had no branch protection at all, so a red check still couldn't block a merge — added a required-status-check rule for the `ci` job with Rachel's go-ahead, closing that gap too.
