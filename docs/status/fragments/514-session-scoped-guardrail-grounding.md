[#514](https://github.com/msdixon/secret-cabinet/issues/514): a researcher can now upload their own material (a Zotero-style bibliography, notes, a PDF) for a session via a new "Sources ▾" menu, and `src/grounding.js` uses it as a fourth citation-checking tier — guardrail-by-default, only verifying/constraining citations the model already made, never expanding what a turn is generated from, and never merged into the shared #35a library. Session-scoped and ephemeral (in-memory, cleared on session delete), with all five of the issue's cost levers: per-claim checking, pre-verification triage, a hard per-session verification-call cap, an ephemeral chunk index as the large-corpus fallback, and size-gated retrieval (keyword search below a char threshold, TF-IDF above it). Verified end-to-end against a real Anthropic API call. [PR #548](https://github.com/msdixon/secret-cabinet/pull/548)
