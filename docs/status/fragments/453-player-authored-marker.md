Scoped [#453](https://github.com/msdixon/secret-cabinet/issues/453) down to its labeling gap: playing as a roster member filed the turn under that member's real `memberId` with nothing distinguishing it from their own words. A beat now carries `playerAuthored: true`, set where `memberId` is resolved for a player-mode turn, and the one export path missing the marker (the scholarly note's Selected Passages) now surfaces it too — verified live against a real convene session. [PR #469](https://github.com/msdixon/secret-cabinet/pull/469).
