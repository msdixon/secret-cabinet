[#584](https://github.com/msdixon/secret-cabinet/issues/584) claimed `public/js/app.js` had zero `// ── Title ──` section-header comments — stale: the automated filer's grep (like this session's first attempt) silently failed on the `─` box-drawing character, missing the 26 headers PR #292/#285 already added, covering all but one of the file's 93 functions. Audit found exactly one real gap — the top-of-file core-state block and `fetchMembers()`, left unheaded since before #292 — closed with one `// ── Core state & roster ──` header; no other change needed.
