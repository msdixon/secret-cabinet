#268's relationship-as-data layer shipped: `src/relationships.js` composes a per-speaker, per-evening fallback seeded from the existing knowledge graph (#22/#84) — reusing its edge shape directly (`type`→register, `label`→shared-history text) rather than a new schema — for any present-member pair a speaker's own character file doesn't already narrate in prose. Verified inert against the current 38-member cast (every seed edge already has prose coverage), activating only for future members or pairs the prose hasn't caught up to. [PR #312](https://github.com/msdixon/secret-cabinet/pull/312)
