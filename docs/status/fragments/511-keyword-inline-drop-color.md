[PR #515](https://github.com/msdixon/secret-cabinet/pull/515) dropped the gold color from `.keyword-inline`, keeping italic only — a color pop on inline `*emphasis*` text still read as an interaction affordance even without the underline #464 already removed. `.inline-link` (amber + underline, still unused pending a real link parser) is untouched. Fixes [#511](https://github.com/msdixon/secret-cabinet/issues/511).
