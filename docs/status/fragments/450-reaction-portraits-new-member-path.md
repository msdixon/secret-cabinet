`POST /api/members` now drafts the decided reaction set (happy/thinking/angry) alongside a new member's baseline portrait, anchored on the baseline via a reference image and gated behind the same `scripts/promote-portrait.js` human-review step — completing #450 alongside the existing-roster backfill (#470, #472, #484). See [#491](https://github.com/msdixon/secret-cabinet/pull/491).
