[PR #525](https://github.com/msdixon/secret-cabinet/pull/525) fixed a retried speaker turn corrupting or permanently wedging the live view: `onSpeakerStart` now re-fires before each retry attempt, not just once per beat, and `witness.js`'s `liveTypingStart` self-evicts a stale unsettled turn instead of relying solely on #423's whole-round-rejection eviction. Closes [#521](https://github.com/msdixon/secret-cabinet/issues/521).
