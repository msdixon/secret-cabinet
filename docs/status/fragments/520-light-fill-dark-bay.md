Lit and filled the room's one dark bay opposite the hearth ([#520](https://github.com/msdixon/secret-cabinet/issues/520)), surfaced once #504's free camera made it explorable: a dedicated accent fixture at that exact wall angle (same #424 `wallSpot()` sconce construction, brighter/wider-range since it has no neighboring sconce to overlap with), and its portrait frame now hangs a second Dorian Gray decor image reusing #479's texture pool — bay 2 stays a plain dark panel so the "intentional empty furnishing" reading still holds for at least one bay. [PR #523](https://github.com/msdixon/secret-cabinet/pull/523).
