A member's room card now holds a short-lived stack of recent beats instead of overwriting its content on every turn: each beat appends a `.room-card-entry`, older entries dim and scroll out of view (reusing #291's existing card scroll), and the whole stack still fades together on inactivity — implementing #287's 2026-08-17 decision (a per-member stack, not a separate history rail) as the deferred second half of #279. [PR #310](https://github.com/msdixon/secret-cabinet/pull/310)
