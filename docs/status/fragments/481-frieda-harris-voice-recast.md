[#481](https://github.com/msdixon/secret-cabinet/issues/481) recast Lady Frieda Harris's ElevenLabs voice: `exsUS4vynmxd379XN4yO` ("Blondie," tagged conversational/casual) read younger and less aristocratic than the character, so her `voiceId` in `prompts/members/roster.json` moves to `2qQJWjw5XdG80GreshqG` ("Eleanor – Gracious and Authoritative," ElevenLabs' own metadata: age `old`, "warm yet commanding presence, suggesting a wealth of experience and high status"), found via the same live `/v1/shared-voices` audition + `/v1/text-to-speech` verification approach as #350. Her `voiceGender`/`voiceDemeanor`/`voiceAccent` tags were already correct per the issue, so this is a one-line `voiceId` override rather than a pool or tagging change — the new ID isn't added to `FALLBACK_VOICE_IDS` since it's specific to this one miscast member, not a general-purpose pool addition.
