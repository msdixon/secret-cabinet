[#357](https://github.com/msdixon/secret-cabinet/issues/357) the room's most-invoked object — the fire, named ~14 times across the runtime prose — now has real fireplace geometry (surround, firebox, five animated flame cones, ember bed) instead of a single 0.4-diameter sphere, and reads the meeting: `LodgeScene.setPassageCount()`/`stirFire()`, wired through `app.js`'s `addLullDivider`, bank it down as passages accumulate and flare it on a "stirs the fire" lull note. Fixed live: a solid chimney-breast box was fully occluding the firebox behind it, same problem `buildBookshelves`' own comment warns about for a solid carcass. Gates [#358](https://github.com/msdixon/secret-cabinet/issues/358)'s room-above-controls layout. [PR #371](https://github.com/msdixon/secret-cabinet/pull/371)
