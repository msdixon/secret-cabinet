Bumped `@anthropic-ai/sdk` 0.127, `babylonjs` 9.27.1, `multer` 2.4.0, `pdf2json` 4.1.0, `eslint` 10.11, `jsdom` 30.1 and `prettier` 3.9.8 (#600), and moved `ci.yml` to `actions/checkout@v7`, `actions/setup-node@v7` and `dorny/paths-filter@v4` (#601). The multer bump carries a medium-severity `diskStorage` DoS fix (GHSA-3pph-fpjx-jg34) that doesn't reach this app, which uses `memoryStorage`; `pdf2json` 4.1.0 raises its own `engines.node` floor to 22.23.2, which CI's `node-version: 22` satisfies. [#600](https://github.com/msdixon/secret-cabinet/issues/600), [#601](https://github.com/msdixon/secret-cabinet/issues/601)
