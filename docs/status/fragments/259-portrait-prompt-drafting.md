[#259](https://github.com/msdixon/secret-cabinet/issues/259) scoped and given a first cut: the in-app "Add Member" flow now also drafts a `STYLE_GUIDE.md`-compliant portrait-generation prompt via the same Claude client already used for character-file drafting, appending it to a new `public/portraits/PENDING-PROMPTS.md`. Generation, resize, and placement stay manual for now, same as every prior wave — not because no image-gen tool exists (a general-purpose one is in fact reachable from a live Claude Code agent session; an initial claim to the contrary in this PR's own description was wrong and corrected), but because that tool is agent-invoked and can't be called from the always-on server process unattended. A session-driven generation step is a viable follow-up, not decided yet. [PR #433](https://github.com/msdixon/secret-cabinet/pull/433)
