[#344](https://github.com/msdixon/secret-cabinet/issues/344) shipped: Past Meetings' dense list rows are now cards in a CSS grid — a 2-4 overlapping portrait avatar-stack, date, and the existing italic one-line excerpt up front, with the six secondary actions (Watch/Reconvene/Thread/Compare/Metrics/Delete) collapsed behind a "more actions" toggle so the glance view stays compact; the publish-privacy disclosure stays unconditionally visible per #38's prior decision. `GET /api/sessions` now returns `memberIds` alongside `members` (index-aligned) so the client can resolve portrait URLs without a second round-trip. [PR #399](https://github.com/msdixon/secret-cabinet/pull/399)
