[#435](https://github.com/msdixon/secret-cabinet/issues/435) closed the loop #259 stopped short of: `POST /api/members` now calls the Gemini API (`gemini-2.5-flash-image`) directly, server-side, generating a real portrait candidate at `public/portraits/candidates/<id>.png` when `GEMINI_API_KEY` is set — no agent session needed, unlike the ElevenLabs MCP path that PR's scoping originally considered. Deliberately stops short of the canonical path: `node scripts/promote-portrait.js <id>` is the human-reviewed resize/place step, keeping `STYLE_GUIDE.md`'s validation requirement intact. Built after a live spike confirmed the mechanics for real (presigned-URL download, `sips` resize, `imageConfig.aspectRatio` orientation control) against actual paid test generations, not assumptions. [PR #433](https://github.com/msdixon/secret-cabinet/pull/433)
