Shipped [#457](https://github.com/msdixon/secret-cabinet/issues/457)'s visual grammar for [#196](https://github.com/msdixon/secret-cabinet/issues/196)/[#456](https://github.com/msdixon/secret-cabinet/pull/456)'s splinter mechanic: both participants' room-card entries get a distinct aside style (dimmed, italic, a plain-text tag) plus a faint SVG connector between their seat anchors, reusing #257's `getSeatScreenPosition` rather than mesh rotation (avatar billboards always face the camera). `thread` now reaches the client over live SSE end-to-end, and a real correctness bug in replay's bracket parsing (the `[Aside — ...]` delimiters leaking as literal text) is fixed along the way. Room-wide dimming and `onSpeakerStart` thread-tagging are named as deliberately deferred, not gaps. Verified live against a real, organically-produced Crowley/Waite splinter session — confirmed via DOM inspection that both cards were tagged and the connector drew between the correct real coordinates. [PR #463](https://github.com/msdixon/secret-cabinet/pull/463).
