Follow-up to [PR #455](https://github.com/msdixon/secret-cabinet/pull/455), which scoped the deferred "per-passage highlighting synced to citations" half of #34 out of its first cut: a new `onCitation` SSE event relays each beat's live-captured citations (#355) to the client, and scene.js's `citeFromBeat()` highlights the matched passage — in the DOM reading panel if open, otherwise a distinct book-glow pulse — only when a citation actually quotes the active provocation document rather than some other real work. [#34](https://github.com/msdixon/secret-cabinet/issues/34), [PR #465](https://github.com/msdixon/secret-cabinet/pull/465).
