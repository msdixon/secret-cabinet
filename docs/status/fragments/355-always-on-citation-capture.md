[#355](https://github.com/msdixon/secret-cabinet/issues/355): citation extraction is now always-on, piggybacked per beat on the existing disposition call (bounded to that turn's text, `memberId` already known) rather than a single opt-in whole-transcript pass — 1 of 11 local sessions had ever been run through Verify Citations before this. `beats[].citations` accumulates on the structured record; `/verify-citations` now only runs the deliberate library/web grounding pass against that capture, and `build-citation-manifest.js` includes every session's citations, grounded or not, rather than only the ones someone remembered to verify. [PR #382](https://github.com/msdixon/secret-cabinet/pull/382)
