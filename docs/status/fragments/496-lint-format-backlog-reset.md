[PR #503](https://github.com/msdixon/secret-cabinet/pull/503) reset the report-only lint/format backlog (1 lint error, 24 drifted files as of 2026-08-30) with `prettier --write` and a one-line unused-var fix — formatting only, no behavior changes. `continue-on-error` stays on `.github/workflows/ci.yml`'s lint/format steps per [#496](https://github.com/msdixon/secret-cabinet/issues/496)'s recommendation, pending a few weeks of observing whether the gate stays clean.
