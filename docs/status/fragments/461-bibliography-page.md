Scoped and shipped [#461](https://github.com/msdixon/secret-cabinet/issues/461): `GET /bibliography`, a real browsable page over the cross-session aggregation `scripts/build-citation-manifest.js`/#356's `src/bibliography.js` already computed but only ever exposed as a raw markdown API response. Scoping calls made directly rather than deferred: kept gated (not added to #379's public-read bypass list) since nothing about the portfolio/grant use case needs it public yet; sits alongside the per-session Scholarly Note export rather than replacing it (different scope — this session vs. the whole project); reuses #356's existing cumulative dedup-by-work unit and Works Cited / Works Referenced / Library-appendix tiers as-is rather than inventing a new axis. Linked from the mantel nav next to Lodge Members. Along the way, wired `renderBibliographyPage` through the same `ROSTER`/`loadBibliographyLibraryEntries()` closure the markdown route already used, so speaker names and the 46-entry library appendix resolve correctly. Verified live against this worktree's real session data. Deferred, named rather than silently dropped: no deep link from a citation's session id back to its `/reading-room/:id` page; no chronological/session-grouped alternate view; no search or pagination (not yet needed at this corpus size). [PR #466](https://github.com/msdixon/secret-cabinet/pull/466).
