Deleted `server.js`'s two dead wrapper functions (`memberBrief`, `openSSE` — both zero-caller copies of code that moved elsewhere) and their now-unused `pipeline` destructures, plus five smaller unused-var lint findings across `lodge.js`, `sessions.js`, and `export.js`, and a `require-yield` fix in `pipeline.test.js`. `npm run lint` and `npm run format:check` are both clean for the first time since CI's report-only gate landed ([#277](https://github.com/msdixon/secret-cabinet/issues/277)) — [#404](https://github.com/msdixon/secret-cabinet/issues/404). Flipping `continue-on-error` off still needs a couple of consecutive clean CI runs first, per the issue.
