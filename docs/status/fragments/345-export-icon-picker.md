Preserve's export menu is now a row of small destination icons (txt / scholarly note / Day One / Ulysses / Obsidian, plus the non-local `.md` fallback) — selecting one shows only that destination's focused config, replacing the old always-all-fields-at-once list and the separate Export Settings drawer it opened into. Presentation only: the underlying export calls, the #37 scholarly gating, and the #331 citation-unlock logic are untouched. [#345](https://github.com/msdixon/secret-cabinet/issues/345)
