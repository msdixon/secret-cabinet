Added a `↻ Recap` button ("what did he just say") that replays only the most recently spoken live turn, reusing the same `renderWitnessBlock` seam live mirroring and replay already share for a fresh card entry, `Voice.speak()` call, and fade scheduling. A deliberately scoped v1 — not the full continuous-rewind/multi-turn scrub #506's title implies, since the live turn queue keeps no addressable history and `Voice.speak()` has no replayable audio object; see [PR #536](https://github.com/msdixon/secret-cabinet/pull/536) for the reasoning. [#506](https://github.com/msdixon/secret-cabinet/issues/506) stays open for the fuller scrub.
