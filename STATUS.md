# STATUS.md

Dated, one-line-per-entry log of what shipped or changed. Newest first.

**Entries arrive as fragments, not direct edits.** When something ships, a decision gets made, or a notable bug gets found/fixed, add a fragment file to [`docs/status/fragments/`](docs/status/fragments/) instead of editing this file directly — see that directory's README for the filename convention and content format. This is the same pattern Towncrier (Python) and Changesets (JS monorepos) use: parallel PR branches all editing the same insertion point in one file were producing avoidable merge conflicts on every overlap, not real logical conflicts, just git seeing concurrent edits to the same spot. `node scripts/assemble-status.js` (run manually, or weekly as a step in the `secret-cabinet-project-doc-checkin` scheduled task) turns pending fragments into dated entries here and deletes the fragments it consumed — see that script's header for details.

**Entries are 1-3 sentences plus the PR/issue link — not a restatement of the PR body.** The PR carries the reasoning, the alternatives considered, and the verification steps; that's what it's for, and it stays attached to the diff permanently. This file is read into context at the start of most sessions, so its size has an ongoing cost beyond scrolling — see [#275](https://github.com/msdixon/secret-cabinet/issues/275), filed after entries drifted to ~4x their prior length in a matter of weeks.

**When this file crosses ~60KB, roll the oldest entries into `docs/status/ARCHIVE-<range>.md`** and leave a pointer here, same as the archives below. Size-triggered, not calendar-triggered — a fixed schedule doesn't track what actually made the file big.

For the *why* behind any entry, check the linked PR/issue first; deeper narrative context (what was tried, what was rejected, collaboration notes) lives in Claude's memory, not here. For current direction and open decisions, see [PROJECT.md](PROJECT.md).

Entries from ~2026-05 through 2026-08-08 archived to [docs/status/ARCHIVE-2026-05-to-2026-08-08.md](docs/status/ARCHIVE-2026-05-to-2026-08-08.md); 2026-08-10 through 2026-08-17 archived to [docs/status/ARCHIVE-2026-08-10-to-2026-08-17.md](docs/status/ARCHIVE-2026-08-10-to-2026-08-17.md).

---

- **2026-09-21** — Bumped the transitive `hono` dependency (pulled in via `@modelcontextprotocol/sdk`'s peer dep on `@hono/node-server`) from 4.13.0 to 4.13.7 via `npm audit fix`, clearing three moderate-severity advisories a scheduled dependency audit flagged (path traversal in `toSSG()`, unbounded `parseBody()` nesting, fragment-confused query parsing). `package-lock.json` only — neither package is a direct dependency. [PR #593](https://github.com/msdixon/secret-cabinet/pull/593)
- **2026-09-21** — [#584](https://github.com/msdixon/secret-cabinet/issues/584) claimed `public/js/app.js` had zero `// ── Title ──` section-header comments — stale: the automated filer's grep (like this session's first attempt) silently failed on the `─` box-drawing character, missing the 26 headers PR #292/#285 already added, covering all but one of the file's 93 functions. Audit found exactly one real gap — the top-of-file core-state block and `fetchMembers()`, left unheaded since before #292 — closed with one `// ── Core state & roster ──` header; no other change needed.
- **2026-09-21** — [#583](https://github.com/msdixon/secret-cabinet/issues/583): extended [#422](https://github.com/msdixon/secret-cabinet/pull/428)'s visit counting to the gated side — convene/cast/round/interject calls from a session authenticated past `applyConveneGate()`, same count/log-only approach, surfaced as a new section in the existing `GET /api/admin/visits` report. Excludes local dev, where every request is authenticated by default and would otherwise swamp the count. [PR #588](https://github.com/msdixon/secret-cabinet/pull/588).
- **2026-09-21** — Renamed the "Reveal/Hide Player Turns ◆" transcript button to "Reveal/Hide Your Turns ◆" (matching the app's existing "Your Turn" modal wording instead of pipeline-internal "player" vocabulary), and made the voice-toggle tooltip name whichever backend (ElevenLabs vs. the browser's own) is actually live instead of a hardcoded, now-stale "Web Speech API" claim. [#582](https://github.com/msdixon/secret-cabinet/issues/582), [PR #591](https://github.com/msdixon/secret-cabinet/pull/591).
- **2026-09-21** — Added a "What is this?" button to the persistent mantel nav that reopens the same preamble banner content, since once `PREAMBLE_DISMISSED_KEY` was set there was previously no way back short of clearing site data. See [#581](https://github.com/msdixon/secret-cabinet/issues/581).
- **2026-09-21** — [#578](https://github.com/msdixon/secret-cabinet/issues/578): `pickNextSpeaker` now weights a pool candidate up when the last speaker's own disposition reads as charged (happy/angry) and the candidate holds a charged #268 relationship tie (rivalry, love, collaboration, intellectual-debt) to them — the room leaning toward the rival/lover/collaborator once the temperature spikes, part 3 of the citation-as-combat redesign (#576, #577). Reuses the existing `reaction` and relationship-graph signals rather than adding new state. [PR #596](https://github.com/msdixon/secret-cabinet/pull/596).
- **2026-09-21** — [#577](https://github.com/msdixon/secret-cabinet/issues/577) — second part of the citation-as-combat redesign: the speaker and disposition prompts now let a member ground a claim via #355's always-on citation tool call without reciting the citation aloud, since the capture already runs independent of narration. That decoupling exposed a real gap: the transcript's citation-verified highlighting matched a citation to its bubble by requiring the quote to literally appear in the spoken turn, which silently broke for a gestured-only citation — fixed with a positional (speaker+beat-index) fallback in `applyCitationFlags`. [PR #592](https://github.com/msdixon/secret-cabinet/pull/592)
- **2026-09-21** — [#576](https://github.com/msdixon/secret-cabinet/issues/576) — first part of the citation-as-combat redesign: reframed `lodge-context.md`'s "receipts are weapons" instruction (and its per-turn echoes in `pipeline-speaker.js`/`lodge-prompts.js`) as "conviction over proof" — a member's stated position no longer needs a citation in the same turn to be complete, though citations stay available when they're the sharpest tool at hand. Individual persona files (Waite's citation-heavy style, Teresa's citation-free register) left untouched as legitimate character variation, not room-level incentive. #577/#578/#579 were sequenced to depend on this landing first. [PR #586](https://github.com/msdixon/secret-cabinet/pull/586)
- **2026-09-21** — [#569](https://github.com/msdixon/secret-cabinet/issues/569): an initial fix for the three moderate-severity portrait-convergence clusters [#558](https://github.com/msdixon/secret-cabinet/issues/558) deferred (abulafia/al-hallaj, arabi/khaldun, eckhart/llull) was generated, reviewed via a before/after Artifact, and rejected — Llull's added combinatorial-wheel prop and face/beard redesign had no identifiable source. All 24 images were reverted to their pre-fix canonical versions instead (cropping abulafia/al-hallaj's borders for roster-wide consistency along the way), and [STYLE_GUIDE.md](../../../public/portraits/STYLE_GUIDE.md) gained a sourcing requirement for invented props/redesigns plus a pre-generation sign-off step for multi-member distinctiveness passes. #569 stays open pending a properly sourced, human-guided redo.
- **2026-09-21** — Added a tracked `pre-commit` hook (`.githooks/pre-commit`) that runs `prettier --write` on staged `.js` files and re-stages the result, closing [#568](https://github.com/msdixon/secret-cabinet/issues/568)'s gap between drift and the CI gate `#544` hardened. Confirmed the hook fires in a harness-created worktree (unlike `post-checkout`, since `pre-commit` triggers on `git commit` itself rather than a checkout event) — but `core.hooksPath` resolves to an absolute path in the primary checkout, so the hook only goes live everywhere once this merges and that checkout runs `git pull`; see `CLAUDE.md`'s new "Pre-commit formatting hook" section.
- **2026-09-13** — Added a Google Books structured check to `scripts/verify-library-sources.js`, closing the gap archive.org/Crossref left for commercially published, in-copyright, non-journal 20th/21st-century books, which have neither identifier type. Surfaced while evaluating InteLex's Yeats letters database (nlx.com) as a persona-grounding source — the database itself didn't fit the license rubric, but the evaluation found a real hole in citation verification. See [#575](https://github.com/msdixon/secret-cabinet/pull/575).
- **2026-09-13** — 2026-09-13 `/cabinet-review` strategic pass, following up on Rachel's reaction to the prior critique: filed a four-part citation-as-combat redesign ([#576](https://github.com/msdixon/secret-cabinet/issues/576)–[#579](https://github.com/msdixon/secret-cabinet/issues/579)), a north-star spike ([#580](https://github.com/msdixon/secret-cabinet/issues/580)), and three smaller UX/infra gaps ([#581](https://github.com/msdixon/secret-cabinet/issues/581)–[#583](https://github.com/msdixon/secret-cabinet/issues/583)). Concluded the pipeline's director/disposition/casting architecture is already genuinely multi-agent and composable — worth naming for the portfolio-pitch purpose — but recommended keeping the *process* as deliberately human-gated as it already is rather than adding more autonomy. No `docs/PRINCIPLES.md` gap found.
- **2026-09-13** — [#571](https://github.com/msdixon/secret-cabinet/issues/571): decided to standardize the roster's member billboards on borderless portraits roster-wide (reinforces an avatar/presence reading in the 3D room, and avoids a border mismatch looking like a bug when several members are seated at once), superseding #531's earlier "no mandate" language. Updated `public/portraits/STYLE_GUIDE.md`'s border/frame rule; 17 currently-bordered members still need conversion, tracked under #571.
- **2026-09-13** — `GET /api/voice/config` now surfaces a `degraded` flag after two consecutive failed ElevenLabs calls — previously a quota-exhaustion incident was invisible, with beats silently falling back to Web Speech — and the default TTS model switched from `eleven_turbo_v2_5` to the cheaper/faster `eleven_flash_v2_5` after side-by-side listening tests. [PR #566](https://github.com/msdixon/secret-cabinet/pull/566).
- **2026-09-13** — [#558](https://github.com/msdixon/secret-cabinet/issues/558) human review: Scholem and Pauli confirmed. Dion Fortune's hokey Tree-of-Life prop was rejected, but a real reference photo turned out to exist for her (Wikidata Q260670), so she was regenerated grounded in it instead. Corbin and Frieda Harris's hokey props were rejected with no equivalent photo available, so both were reverted to their pre-#558 portraits — filed as [#574](https://github.com/msdixon/secret-cabinet/issues/574) for a real fix.
- **2026-09-13** — [#558](https://github.com/msdixon/secret-cabinet/issues/558): fixed cross-member portrait convergence for the five most severe cases (corbin, scholem, pauli, frieda-harris, dion-fortune) — each now gets a grounded, concrete distinguishing detail (real photo inspection or a biographical trade-marker prop) instead of the generic attire-only template that let members sharing an era/profession/attire read as near-identical. Three moderate-severity clusters (abulafia/al-hallaj, arabi/khaldun, eckhart/llull) were deliberately deferred, filed as [#569](https://github.com/msdixon/secret-cabinet/issues/569).
- **2026-09-13** — [#558](https://github.com/msdixon/secret-cabinet/issues/558) follow-up: Scholem's and Pauli's regenerated base portraits came back with a radial vignette (bright-cornered halo) that the rest of the roster doesn't have — the fix script's composition prompt was missing an anti-vignette clause `REACTION_STYLE_SUFFIX` already carries since the #450 fix. Regenerated base + all three reactions for both members with an explicit anti-vignette instruction (`scripts/fix-558-vignette-fixup.js`); shipped as an additional commit on the still-open [#570](https://github.com/msdixon/secret-cabinet/pull/570).
- **2026-09-13** — [#557](https://github.com/msdixon/secret-cabinet/issues/557): standardized `roster.json`'s inconsistent `name` field (7 members were last-name-only) to full first+last, matching the convention `docs/MANIFEST.md` and each character file's own header already used. Roster array order was left unchanged (not functionally load-bearing anywhere audited); the old short forms were added to each renamed member's `aliases` so `src/relationships.js`'s hand-written-mention detection still matches the many character files that bold short-form names.
- **2026-09-13** — Bumped `babylonjs` (9.23.0 → 9.25.0), `eslint` (10.9.1 → 10.10.0), `globals` (17.11.0 → 17.12.0), and `@anthropic-ai/sdk` (0.122.0 → 0.124.0), following the same pattern as the 2026-09-07 batch: full test suite and lint pass, the 3D room still renders and orbits under the new babylonjs, and `npm audit` shows no new advisories from any of the four. See [#553](https://github.com/msdixon/secret-cabinet/issues/553), [#554](https://github.com/msdixon/secret-cabinet/issues/554), [#555](https://github.com/msdixon/secret-cabinet/issues/555), [#556](https://github.com/msdixon/secret-cabinet/issues/556).
- **2026-09-13** — [#546](https://github.com/msdixon/secret-cabinet/issues/546): audited the roster for the text-only-likeness gap [#541](https://github.com/msdixon/secret-cabinet/issues/541) fixed for Crowley. Promoted five corrected members (Warburg, Sun Ra, Randolph, Éliphas Lévi, Pamela Colman Smith), two rounds deep in three cases after review caught prompt text misdescribing the reference photo itself (invented attire, complexion/nose drift, invented glasses); rejected Blavatsky and Pauli outright, current portraits kept. Details and per-member notes in `public/portraits/STYLE_GUIDE.md`'s Changelog.
- **2026-09-13** — [#543](https://github.com/msdixon/secret-cabinet/issues/543) — `public/js/scene/scene.js`'s 2063 lines / ~46 functions had zero section-header comments, the same gap #285/#292 found and mostly fixed with comments (not a split) in `app.js`. Audited for a `speaker.js`-style clean extraction seam and found none — every function, including the handful that look pure at a glance, touches the module's closure state — so this follows #292's precedent exactly: 10 `// ── Title ──` headers added, no code moved ([PR #551](https://github.com/msdixon/secret-cabinet/pull/551)).
- **2026-09-13** — [#542](https://github.com/msdixon/secret-cabinet/issues/542) (phase 2 of [#512](https://github.com/msdixon/secret-cabinet/issues/512)): added `scripts/report-observed-length-tendency.js`, an advisory-only report comparing each member's static `LENGTH_TENDENCY_OVERRIDES` entry against their observed average words-per-spoken-turn, gated at 20 real turns to avoid comparing on cold-start noise. Deliberately doesn't write back into `tuning.js` — the two design blockers #542 was filed against turned out resolved (#539's clarification that `LENGTH_WEIGHT` only affects speaker selection, not turn length; #506 shipping without depending on static tuning values), but cold start is real, so this stays a report a person reads rather than an auto-apply mechanism. When/whether to promote it is an open question, not decided here. [PR #573](https://github.com/msdixon/secret-cabinet/pull/573)
- **2026-09-13** — Live re-measured [#552](https://github.com/msdixon/secret-cabinet/pull/552)'s `TYPICAL_TURN_WORDS` anchor against real `claude-sonnet-5` calls (same 3-pair/9-trial methodology as [#513](https://github.com/msdixon/secret-cabinet/issues/513) phase 3): the anchor is a real, partial fix (200w → 157w, −21.5%), but a further test showed pushing the same number lower (60 vs. the shipped 90) plateaus rather than closing the rest of the gap to phase 1's 88w baseline. Closed [#539](https://github.com/msdixon/secret-cabinet/issues/539) on this measured record and filed [#561](https://github.com/msdixon/secret-cabinet/issues/561) to carry the residual "what lever, not what number" question forward separately — see `BREVITY-BASELINE-REPORT.md`'s phase 4.
- **2026-09-13** — Gave the speaker prompt a concrete turn-length anchor (`tuning.js`'s new `TYPICAL_TURN_WORDS`, 90) after [#513](https://github.com/msdixon/secret-cabinet/issues/513) phase 3 found the un-nudged floor had roughly doubled (88w → 200w) once the model default switched to `claude-sonnet-5` ([#406](https://github.com/msdixon/secret-cabinet/issues/406)) — the prior guidance was purely permission-shaped, with no number for the model to react to outside the already-crowded case. Unverified against a live re-measurement (no API key in the environment that made the change); [#539](https://github.com/msdixon/secret-cabinet/issues/539) stays open pending that.
- **2026-09-13** — [#538](https://github.com/msdixon/secret-cabinet/issues/538) — Teresa of Ávila's reaction portraits (`teresa-happy`/`teresa-thinking`/`teresa-angry`) regenerated to match her baseline's visual register (borderless, near-black background, painterly), deferred from [#531](https://github.com/msdixon/secret-cabinet/issues/531) since the baseline itself is also used elsewhere in the app; `teresa.png` is unchanged ([PR #563](https://github.com/msdixon/secret-cabinet/pull/563)).
- **2026-09-13** — Extended the `#506` recap mechanism ([PR #536](https://github.com/msdixon/secret-cabinet/pull/536)) from a single-slot "last turn" replay into a bounded, steppable turn history: `⏮ Prev turn` walks backward through already-spoken live turns one at a time, `Next turn ⏭` walks forward again, both reusing the same `renderWitnessBlock` seam. Settles the issue's open design question in favor of discrete previous-turn jumps over continuous Roku-style rewind — `Voice.speak()` has no persisted/replayable audio object, so there's no audio timeline to scrub, only turn boundaries to jump between (button wording deliberately avoids a time-based "back 15s" convention for the same reason). [#506](https://github.com/msdixon/secret-cabinet/issues/506)
- **2026-09-07** — Added a browser-tab favicon — a single watching eye in the app's warm sepia/etching register, chosen over hearth-flame/sigil/monogram alternatives as classic esoteric-society iconography that reads as "secret" at a glance. See [#549](https://github.com/msdixon/secret-cabinet/pull/549).
- **2026-09-07** — [#541](https://github.com/msdixon/secret-cabinet/issues/541): base portraits were generated from text-only prompts with no photographic anchor, letting likeness drift from the model's own training association — confirmed for Crowley, whose base read too young/slim vs. his real heavier, jowlier "Great Beast" prime. Sourced a plain archival photo and added a `referenceImages`-anchoring mechanism to base-portrait generation (`buildLikenessAnchoredBasePrompt`, `public/portraits/likeness-refs/`), regenerated and promoted Crowley's base and reaction set against it, and filed [#546](https://github.com/msdixon/secret-cabinet/issues/546) to audit the rest of the roster for the same gap.
- **2026-09-07** — [#531](https://github.com/msdixon/secret-cabinet/issues/531): audited all 37 promoted reaction sets against each member's own baseline (border, outfit, tone, crop — not just per-image quality) and regenerated 13 drifted images across 8 members (Crowley, Waite, Blavatsky full or partial sets; Frieda Harris, Dion Fortune, Moina Mathers, Jung, Böhme `thinking`-only). Added explicit border/frame and camera-distance/crop rules to `STYLE_GUIDE.md`'s baseline register so future batches state this instead of relying on the reference image alone. Teresa's baseline-vs-reactions style gap and the roster-wide bordered-vs-borderless split are documented as deliberate follow-ups, not fixed here.
- **2026-09-07** — [PR #532](https://github.com/msdixon/secret-cabinet/pull/532) fixed room speech cards fading before their audio finished: `scheduleCardFade` now waits on the same `Voice.speak()` promise `#336`'s pacing fix already awaits, not just `witnessReadingTime`'s WPM estimate, so a card is held until whichever is longer — the WPM estimate or real audio completion — before its grace period starts. Live-verified with real ElevenLabs audio, not just unit tests. Closes [#529](https://github.com/msdixon/secret-cabinet/issues/529).
- **2026-09-07** — Voice now prefetches the next speech beat's ElevenLabs audio while the current one is still playing, closing the ~650-1150ms text-before-audio gap `renderWitnessBlock` had left open in both replay and live playback. Live-verified against a real ElevenLabs session: post-prefetch beats landed at a consistent ~260-270ms gap versus the first beat's unavoidable ~1240ms cold-start. See [PR #533](https://github.com/msdixon/secret-cabinet/pull/533).
- **2026-09-07** — Bumped `@anthropic-ai/sdk`, `babylonjs`, `eslint`, and `multer` per the 2026-09-01 dependency audit, verifying the 3D room still renders/orbits under the new babylonjs and the upload route still passes under the new multer. The multer bump turned out to fix three high-severity DoS advisories `npm audit` hadn't yet surfaced, updating #510's original "not security-motivated" read. See [#507](https://github.com/msdixon/secret-cabinet/issues/507), [#508](https://github.com/msdixon/secret-cabinet/issues/508), [#509](https://github.com/msdixon/secret-cabinet/issues/509), [#510](https://github.com/msdixon/secret-cabinet/issues/510), [PR #522](https://github.com/msdixon/secret-cabinet/pull/522).
- **2026-09-07** — [PR #525](https://github.com/msdixon/secret-cabinet/pull/525) fixed a retried speaker turn corrupting or permanently wedging the live view: `onSpeakerStart` now re-fires before each retry attempt, not just once per beat, and `witness.js`'s `liveTypingStart` self-evicts a stale unsettled turn instead of relying solely on #423's whole-round-rejection eviction. Closes [#521](https://github.com/msdixon/secret-cabinet/issues/521).
- **2026-09-07** — Lit and filled the room's one dark bay opposite the hearth ([#520](https://github.com/msdixon/secret-cabinet/issues/520)), surfaced once #504's free camera made it explorable: a dedicated accent fixture at that exact wall angle (same #424 `wallSpot()` sconce construction, brighter/wider-range since it has no neighboring sconce to overlap with), and its portrait frame now hangs a second Dorian Gray decor image reusing #479's texture pool — bay 2 stays a plain dark panel so the "intentional empty furnishing" reading still holds for at least one bay. [PR #523](https://github.com/msdixon/secret-cabinet/pull/523).
- **2026-09-07** — Gave the 3D room's one remaining bare mid-bay slot a sealed door — an architrave, two panelled leaves, and a keyholeless brass escutcheon, placed on the harder-to-find side of the default camera framing so it rewards the free-drag orbit #504 introduced rather than being handed to the resting shot. [#519](https://github.com/msdixon/secret-cabinet/issues/519), [#535](https://github.com/msdixon/secret-cabinet/pull/535)
- **2026-09-07** — [PR #524](https://github.com/msdixon/secret-cabinet/pull/524) fixed `speakViaElevenLabs()` setting `audio.playbackRate` before `audio.src` — the media-element load algorithm resets `playbackRate` to `defaultPlaybackRate` on `.src` assignment, so ElevenLabs audio always played at native 1x regardless of the Playback Speed control. Verified live against the real ElevenLabs backend, and resolves mechanism #2 from [#505](https://github.com/msdixon/secret-cabinet/issues/505)'s diagnosis; #505 stays open for its separate per-beat text-before-audio latency finding. Fixes [#518](https://github.com/msdixon/secret-cabinet/issues/518).
- **2026-09-07** — [#514](https://github.com/msdixon/secret-cabinet/issues/514): a researcher can now upload their own material (a Zotero-style bibliography, notes, a PDF) for a session via a new "Sources ▾" menu, and `src/grounding.js` uses it as a fourth citation-checking tier — guardrail-by-default, only verifying/constraining citations the model already made, never expanding what a turn is generated from, and never merged into the shared #35a library. Session-scoped and ephemeral (in-memory, cleared on session delete), with all five of the issue's cost levers: per-claim checking, pre-verification triage, a hard per-session verification-call cap, an ephemeral chunk index as the large-corpus fallback, and size-gated retrieval (keyword search below a char threshold, TF-IDF above it). Verified end-to-end against a real Anthropic API call. [PR #548](https://github.com/msdixon/secret-cabinet/pull/548)
- **2026-09-07** — [PR #517](https://github.com/msdixon/secret-cabinet/pull/517) measured a real baseline for [#513](https://github.com/msdixon/secret-cabinet/issues/513) (phase 1, not the lever): `scripts/measure-brevity-baseline.js` found 88 words average spoken-turn length across the 11 local sessions, with all of them predating #355's per-beat citation capture, so citation figures there are a documented heuristic lower bound rather than exact. #513 stays open for the phase-2 lever, informed by this baseline and by #512's outcome.
- **2026-09-07** — [PR #530](https://github.com/msdixon/secret-cabinet/pull/530) shipped the phase-2 lever for [#513](https://github.com/msdixon/secret-cabinet/issues/513): a `TANGENT_NUDGE_CHANCE` coin flip (`tuning.js`/`pipeline-speaker.js`, shaped like `shouldSplinter`) that tells a speaker a short, tangent, or citation-free turn is a complete turn tonight. Live A/B against the real model showed a 269→17 and 247→31 word drop on identical context when nudged, still in character. Chosen over widening `LENGTH_TENDENCY_OVERRIDES` (deferred to #512's outcome) or a structural banter beat (bigger lift, worth it only if this proves insufficient); #513 stays open pending a real-session re-measurement of the baseline.
- **2026-09-07** — Re-measured [#513](https://github.com/msdixon/secret-cabinet/issues/513)'s tangent nudge against real live sessions on current `main`: a controlled on/off A/B against the model actually in production (`claude-sonnet-5`) confirmed the lever still cuts turn length by ~56% when it fires, matching phase 2's original validation. The raw "room reads longer than phase 1" signal turned out to be a separate, unrelated confound — the `claude-sonnet-4-6`→`claude-sonnet-5` default switch ([#406](https://github.com/msdixon/secret-cabinet/issues/406)) roughly doubled the un-nudged floor — so closing #513 on the nudge's own measured record rather than raising `TANGENT_NUDGE_CHANCE` against the wrong variable, and filing [#539](https://github.com/msdixon/secret-cabinet/issues/539) for the model-verbosity question separately.
- **2026-09-07** — [#512](https://github.com/msdixon/secret-cabinet/issues/512) phase 1 — `LENGTH_TENDENCY_OVERRIDES` in `src/tuning.js` is now derived from each member's own #187 voice exemplar (average words/sentence, bucketed by quartile) via `scripts/derive-length-tendency.js`, replacing a hand-tagged list that only ever covered Crowley and Yeats. Still plain static config, computed once and checked in — the adaptive, observed-turn-length version is deliberately deferred as [#542](https://github.com/msdixon/secret-cabinet/issues/542).
- **2026-09-07** — [PR #515](https://github.com/msdixon/secret-cabinet/pull/515) dropped the gold color from `.keyword-inline`, keeping italic only — a color pop on inline `*emphasis*` text still read as an interaction affordance even without the underline #464 already removed. `.inline-link` (amber + underline, still unused pending a real link parser) is untouched. Fixes [#511](https://github.com/msdixon/secret-cabinet/issues/511).
- **2026-09-07** — Added a `↻ Recap` button ("what did he just say") that replays only the most recently spoken live turn, reusing the same `renderWitnessBlock` seam live mirroring and replay already share for a fresh card entry, `Voice.speak()` call, and fade scheduling. A deliberately scoped v1 — not the full continuous-rewind/multi-turn scrub #506's title implies, since the live turn queue keeps no addressable history and `Voice.speak()` has no replayable audio object; see [PR #536](https://github.com/msdixon/secret-cabinet/pull/536) for the reasoning. [#506](https://github.com/msdixon/secret-cabinet/issues/506) stays open for the fuller scrub.
- **2026-09-07** — Re-verified [#505](https://github.com/msdixon/secret-cabinet/issues/505)'s audio/text/speed sync diagnosis against current `main` post-[#524](https://github.com/msdixon/secret-cabinet/pull/524): the speed-multiplier convergence mechanism is confirmed fixed, but the per-beat text-before-audio latency (~650-1150ms, text renders before `Voice.speak()`'s fetch resolves) is confirmed still present and unchanged. Closed #505 and filed the remaining latency gap as its own follow-up, [#527](https://github.com/msdixon/secret-cabinet/issues/527).
- **2026-09-07** — Added drag-to-orbit camera control to the 3D room, live only during idle/lull states or before a session starts — the moment a speaker turn or document-inspect triggers a scripted camera move, free control yields immediately and only returns once that move's ease finishes and the room is still idle. See [#504](https://github.com/msdixon/secret-cabinet/issues/504), [PR #516](https://github.com/msdixon/secret-cabinet/pull/516).
- **2026-09-07** — A day after [PR #503](https://github.com/msdixon/secret-cabinet/pull/503) cleared it, the report-only lint/format backlog had already drifted again — 4 files reformatted by `#521`/`#513`/playbackRate-fix commits landing right after. This PR reformats those 4 files with `npm run format` (no lint errors this time); `continue-on-error` stays on per [#496](https://github.com/msdixon/secret-cabinet/issues/496)'s recommendation, and the fast regrowth is itself more data for that issue's "does it stay clean" question.
- **2026-09-07** — [PR #503](https://github.com/msdixon/secret-cabinet/pull/503) reset the report-only lint/format backlog (1 lint error, 24 drifted files as of 2026-08-30) with `prettier --write` and a one-line unused-var fix — formatting only, no behavior changes. `continue-on-error` stays on `.github/workflows/ci.yml`'s lint/format steps per [#496](https://github.com/msdixon/secret-cabinet/issues/496)'s recommendation, pending a few weeks of observing whether the gate stays clean.
- **2026-09-07** — [PR #544](https://github.com/msdixon/secret-cabinet/pull/544) closes [#496](https://github.com/msdixon/secret-cabinet/issues/496) for good: the report-only lint/format backlog had regrown and been manually reset three times in a week because `continue-on-error: true` made the CI step report success regardless of its actual result, so a PR reintroducing drift still showed green. Fixed the 2 lint errors and 2 format-drifted files on `main`, confirmed clean against a fresh CI run, then dropped `continue-on-error` from both steps so future drift fails the build instead of passing silently.
- **2026-09-07** — [#483](https://github.com/msdixon/secret-cabinet/issues/483): `angry` reaction portraits now have two prompt registers — the existing bared-teeth snarl (default) and a new controlled, closed-mouth, icy-anger alternative — selected per member via a curated table in `src/portrait-generation.js`, seeded with the one member named in the issue (Adorno). No already-reviewed member's reaction set was regenerated. See [PR #534](https://github.com/msdixon/secret-cabinet/pull/534).
- **2026-09-01** — State-of-the-app review (`/cabinet-review`), code-only — this session's scheduled-task-origin transport blocks the dev server, so no live walkthrough. Found the room reads essayistic/citation-heavy by design with nothing structurally weighted toward brevity, and that the length-tendency roster override (`tuning.js`) has aged out (only Crowley/Yeats tagged `'expansive'`). Filed [#511](https://github.com/msdixon/secret-cabinet/issues/511) (drop `.keyword-inline`'s color), [#512](https://github.com/msdixon/secret-cabinet/issues/512) (spike: self-tuning length/register), [#513](https://github.com/msdixon/secret-cabinet/issues/513) (a structural brevity lever), and [#514](https://github.com/msdixon/secret-cabinet/issues/514) (spike: user-supplied grounding/Zotero); added a diagnostic note to the pre-existing sync-gap issue, [#505](https://github.com/msdixon/secret-cabinet/issues/505).
- **2026-09-01** — `src/bibliography.js`'s `renderBibliographyPage` HTML-rendering path (#461, lines 194-420) had zero test coverage — the worst-covered file in the suite at 53.61% line / 79.63% branch / 70.83% function. [PR #499](https://github.com/msdixon/secret-cabinet/pull/499) added a parallel `renderBibliographyPage` test suite mirroring the existing `buildBibliography` tests, covering verdict-pill styling, the grounded-link fallback chain, and HTML-escaping of citations/notes/library entries, bringing the file to 100% line / 87.10% branch / 100% function coverage. See [#495](https://github.com/msdixon/secret-cabinet/issues/495).
- **2026-09-01** — [#494](https://github.com/msdixon/secret-cabinet/issues/494): removed `NON_ROSTER_SPEAKER_IDS` from `public/js/record.js` — a derived convenience array with zero callers anywhere in the repo, confirmed by a fresh grep before deleting. [PR #498](https://github.com/msdixon/secret-cabinet/pull/498)
- **2026-09-01** — [#474](https://github.com/msdixon/secret-cabinet/issues/474): a generic, non-likeness silhouette portrait (`public/portraits/generic-silhouette.png`) now covers two of the issue's three fallback cases — a seat with no baseline portrait at all (previously vanished, now shows the silhouette) and Amadou Bamba's reaction set, routed straight to it rather than ever requesting a personalized `bamba-<reaction>.png` (his baseline is unaffected). The third case — an ordinary member's specific reaction pair missing — keeps its existing fallback to the member's own baseline portrait, a real likeness beating a generic one when we have it. [PR #500](https://github.com/msdixon/secret-cabinet/pull/500).
- **2026-09-01** — [#458](https://github.com/msdixon/secret-cabinet/issues/458) added the second splinter trigger #196/PR #456 deferred: the director can now propose a `splinterPair` directly on its existing `select_speakers` call (the `windingDown`/`lullNote` pattern), opening a private aside — "Crowley leans toward Coleman-Smith" — with no live #203 interrupt-intent signal needed. It shares the reactive trigger's `MAX_SPLINTERS_PER_PASSAGE` cap and budget-headroom gate, with no rng roll of its own: the director's own prompt-level restraint is the gate instead. [PR #501](https://github.com/msdixon/secret-cabinet/pull/501)
- **2026-08-31** — [#482](https://github.com/msdixon/secret-cabinet/issues/482) fixed Paracelsus's awkward mid-phrase pauses: `stripForSpeech()`'s output on representative italicized-Latin/em-dash dialogue lines checked out clean, ruling out a text-formatting cause, so the culprit was the ElevenLabs voice itself — `zlatCM6nK59gyedHFFxn` ("Christian Plasa," ElevenLabs-tagged for advertisement/ad-read use) measured 0.51s average internal silence gaps (up to 28% of total audio as dead air) via live `/v1/text-to-speech` + `ffmpeg silencedetect` comparison, the same live-audition approach #350 and #481 used. `voiceId` moves to `lpTmFkDweYy2qYksP2Ts` ("Rufus – Action Hero," German-accented, ElevenLabs' own description: "intense narration... combative dialogue"), which roughly halved silence duration on the same test lines while matching the character's already-correct `intense` demeanor tag better than the ad-read original. [PR #492](https://github.com/msdixon/secret-cabinet/pull/492)
- **2026-08-31** — [#481](https://github.com/msdixon/secret-cabinet/issues/481) recast Lady Frieda Harris's ElevenLabs voice: `exsUS4vynmxd379XN4yO` ("Blondie," tagged conversational/casual) read younger and less aristocratic than the character, so her `voiceId` in `prompts/members/roster.json` moves to `2qQJWjw5XdG80GreshqG` ("Eleanor – Gracious and Authoritative," ElevenLabs' own metadata: age `old`, "warm yet commanding presence, suggesting a wealth of experience and high status"), found via the same live `/v1/shared-voices` audition + `/v1/text-to-speech` verification approach as #350. Her `voiceGender`/`voiceDemeanor`/`voiceAccent` tags were already correct per the issue, so this is a one-line `voiceId` override rather than a pool or tagging change — the new ID isn't added to `FALLBACK_VOICE_IDS` since it's specific to this one miscast member, not a general-purpose pool addition.
- **2026-08-31** — [#479](https://github.com/msdixon/secret-cabinet/issues/479): the 3D room's empty gilt wall frame nearest the fireplace now hangs a static, non-animated Dorian Gray portrait — generic room decor generated against `STYLE_GUIDE.md`'s baseline register, not a roster member or part of the #450 reaction-portrait system. Extended to 8 age/decay variants (`public/portraits/decor/dorian-gray-01.png`..`-08.png`, one picked at random each time the room loads) as a literary easter egg echoing the novel's aging-portrait conceit — deliberately not tied to any room logic or persistence. Both live outside the member-id-keyed top-level `portraits/` namespace. [PR #493](https://github.com/msdixon/secret-cabinet/pull/493)
- **2026-08-31** — Fixed ElevenLabs TTS reading uniformly too slowly across every member ([#478](https://github.com/msdixon/secret-cabinet/issues/478)): `src/routes/voice.js` sent no `voice_settings.speed` at all, leaving pace at whatever ElevenLabs' bare per-voice default happened to be. Added `voice_settings: { speed: 1.1 }` to the request body, folding speed into the disk-cache key so the fix also reaches previously-cached lines rather than silently serving stale slow audio. The 1.1 default came from live A/B testing against the real ElevenLabs API (repeated trials to average out its own generation-to-generation timing variance) — consistently ~6-7% shorter audio than the 1.0/no-param baseline, comfortably inside the documented `[0.7, 1.2]` range.
- **2026-08-31** — Fixed [#477](https://github.com/msdixon/secret-cabinet/issues/477): `audio.playbackRate` was set once, at `<audio>`-element creation, so a mid-utterance speed-control click silently applied starting next beat only. `voice.js` now exposes `Voice.updateSpeed()`, called from `witness.js`'s `setSpeed()`, which reassigns `playbackRate` on whatever ElevenLabs clip is already playing — the Web Speech fallback is unchanged since `SpeechSynthesisUtterance.rate` can't be live-updated once speech starts. Verified live against the real ElevenLabs backend by cycling the actual speed button mid-utterance. [PR #490](https://github.com/msdixon/secret-cabinet/pull/490)
- **2026-08-31** — Fixed room speech cards opening scrolled to the tail of a long passage, burying its opening lines behind the sticky `.speaker-name` header — `witness.js`'s `scrollCardToLatest()` was jumping to `scrollHeight` unconditionally, right for #287's stacked-beats/actively-typing cases but wrong for a beat that lands complete in one shot (replay's path). Added a `scrollCardToEntryStart()` used only for freshly-appended entries; live-typing tail-follow and #287's behavior verified unaffected. [#476](https://github.com/msdixon/secret-cabinet/issues/476), [PR #485](https://github.com/msdixon/secret-cabinet/pull/485).
- **2026-08-31** — Fixed the lull ("Continue"/"Let it end") prompt appearing while dialogue or audio was still playing ([#475](https://github.com/msdixon/secret-cabinet/issues/475)): `runLullLoop()` in `app.js` rendered the lull as soon as `streamPost()` resolved, which is only the *server* finishing generation — `witness.js`'s `#400` live-turn queue was often still pacing that same text/audio onto the stage. Added `waitForLiveQueueDrain()` to `witness.js`, resolved from the same three spots that already bring the queue to empty, and gated the lull's appearance on it — the identical signal `collapseStage()` already used on the dismissal side. Verified live against a real voice-on convene session: the lull correctly stayed withheld for the full duration of a long paced passage and appeared only once the queue actually drained.
- **2026-08-31** — Inline `*emphasis*` text (`.keyword-inline`) shared its amber color and a dotted border-bottom with the app's real links, making it look clickable; it now renders in a distinct gold with no underline, and a reserved `.inline-link` class (amber + underline) is in place for when inline hyperlink parsing is actually built. [#464](https://github.com/msdixon/secret-cabinet/pull/464)
- **2026-08-31** — Scoped and shipped [#461](https://github.com/msdixon/secret-cabinet/issues/461): `GET /bibliography`, a real browsable page over the cross-session aggregation `scripts/build-citation-manifest.js`/#356's `src/bibliography.js` already computed but only ever exposed as a raw markdown API response. Scoping calls made directly rather than deferred: kept gated (not added to #379's public-read bypass list) since nothing about the portfolio/grant use case needs it public yet; sits alongside the per-session Scholarly Note export rather than replacing it (different scope — this session vs. the whole project); reuses #356's existing cumulative dedup-by-work unit and Works Cited / Works Referenced / Library-appendix tiers as-is rather than inventing a new axis. Linked from the mantel nav next to Lodge Members. Along the way, wired `renderBibliographyPage` through the same `ROSTER`/`loadBibliographyLibraryEntries()` closure the markdown route already used, so speaker names and the 46-entry library appendix resolve correctly. Verified live against this worktree's real session data. Deferred, named rather than silently dropped: no deep link from a citation's session id back to its `/reading-room/:id` page; no chronological/session-grouped alternate view; no search or pagination (not yet needed at this corpus size). [PR #466](https://github.com/msdixon/secret-cabinet/pull/466).
- **2026-08-31** — Verify Citations now shows a "N of M citations grounded — Export Source Notes →" confirmation in its own row below the buttons after a verify pass completes, linking straight into the Preserve > Scholarly Note destination it feeds. [#331](https://github.com/msdixon/secret-cabinet/issues/331)'s tooltip-on-a-different-button attempt didn't surface at the point of action; this does. [#460](https://github.com/msdixon/secret-cabinet/issues/460), [PR #467](https://github.com/msdixon/secret-cabinet/pull/467)
- **2026-08-31** — Shipped [#457](https://github.com/msdixon/secret-cabinet/issues/457)'s visual grammar for [#196](https://github.com/msdixon/secret-cabinet/issues/196)/[#456](https://github.com/msdixon/secret-cabinet/pull/456)'s splinter mechanic: both participants' room-card entries get a distinct aside style (dimmed, italic, a plain-text tag) plus a faint SVG connector between their seat anchors, reusing #257's `getSeatScreenPosition` rather than mesh rotation (avatar billboards always face the camera). `thread` now reaches the client over live SSE end-to-end, and a real correctness bug in replay's bracket parsing (the `[Aside — ...]` delimiters leaking as literal text) is fixed along the way. Room-wide dimming and `onSpeakerStart` thread-tagging are named as deliberately deferred, not gaps. Verified live against a real, organically-produced Crowley/Waite splinter session — confirmed via DOM inspection that both cards were tagged and the connector drew between the correct real coordinates. [PR #463](https://github.com/msdixon/secret-cabinet/pull/463).
- **2026-08-31** — A 2026-08-26 design critique of #348 (PR #440, which had shipped a status button mirroring "Play as" next to Convene the Lodge) found that button visually diluted the two real convene actions and reused the additive-action dashed-border convention for a non-action. Removed it, folding its live-state readout into a new "You" chip in members-grid (same chip styling as any unseated member) whose click reveals the mode/member/custom-identity fields inline in place of the old collapsed details panel. [#348](https://github.com/msdixon/secret-cabinet/issues/348), [PR #454](https://github.com/msdixon/secret-cabinet/pull/454).
- **2026-08-31** — Scoped [#453](https://github.com/msdixon/secret-cabinet/issues/453) down to its labeling gap: playing as a roster member filed the turn under that member's real `memberId` with nothing distinguishing it from their own words. A beat now carries `playerAuthored: true`, set where `memberId` is resolved for a player-mode turn, and the one export path missing the marker (the scholarly note's Selected Passages) now surfaces it too — verified live against a real convene session. [PR #469](https://github.com/msdixon/secret-cabinet/pull/469).
- **2026-08-31** — [#452](https://github.com/msdixon/secret-cabinet/issues/452) shipped, closing out the reaction-portrait thread #449 opened: `scene.js`'s billboards now actually swap texture per member per #451's live `reaction` signal, via a new `applyReactionTexture` and `reactionTextures` cache mirroring the existing `portraitTextures` pattern. Falls back to the member's default portrait for `'none'` and for any member/reaction pair not yet generated (25/38 as of #470/#472) — waits for a confirmed successful load rather than optimistically assigning and correcting on error, since with most pairs currently missing that would flash blank on nearly every disposition update. Hard swap, not a cross-fade: Babylon has no built-in texture interpolation and the codebase had no cross-fade precedent to extend. Verified live against a real scene — a covered member's (Crowley) reaction image loaded and swapped, an uncovered member's (Jung) 404'd cleanly with the seat staying on their default portrait. [PR #473](https://github.com/msdixon/secret-cabinet/pull/473)
- **2026-08-31** — [#451](https://github.com/msdixon/secret-cabinet/issues/451) shipped: the `reaction` tag #449's spike added to the disposition tool call (PR #470) now reaches the client, following #360's `pool`/`disposition` SSE path (PR #391) — `pipeline.js`'s `onDisposition` callback, `convene.js`'s SSE payload, and `scene.js`'s new `memberReactions` map (`getReaction(memberId)`) all carry it through. Decided a reaction persists per member until their own next disposition update, the same lifetime `waitingOnMemberId` already has, rather than a timed decay or turn-scoped flash. Storage only, verified live over real SSE — the texture swap itself is [#452](https://github.com/msdixon/secret-cabinet/issues/452), still separately scoped. [PR #471](https://github.com/msdixon/secret-cabinet/pull/471)
- **2026-08-31** — [#450](https://github.com/msdixon/secret-cabinet/issues/450) batch 2: generated and promoted the happy/thinking/angry reaction set for 10 more roster members (Waite, Coleman-Smith, Blavatsky, Lévi, Ibn Arabi, Maud Gonne, Llull, Ibn Khaldun, Dee, Warburg) past the [#470](https://github.com/msdixon/secret-cabinet/pull/470) pilot cohort. Several first-pass candidates drifted from their baseline portrait on text description alone; fixed by extending the Gemini pipeline (`src/portrait-generation.js`) to attach each member's baseline portrait as a reference image, which anchored likeness/attire far more reliably than prose. See [#472](https://github.com/msdixon/secret-cabinet/pull/472).
- **2026-08-31** — [#450](https://github.com/msdixon/secret-cabinet/issues/450) batch 3: generated and promoted the happy/thinking/angry reaction set for the remaining 24 roster members past batch 2, applying the reference-image + expression-override approach from the start rather than discovering the same drift mid-batch — first-pass quality was markedly better as a result. Rachel's review still caught a beard-drift likeness bug (Randolph's `happy`) and nine `thinking` candidates reading too close to baseline, both fixed. Amadou Bamba's set is held back deliberately, not promoted — Rachel's call was that his reaction set needs a non-likeness stand-in rather than personalized expressions, tracked as [#474](https://github.com/msdixon/secret-cabinet/issues/474). See [#484](https://github.com/msdixon/secret-cabinet/pull/484).
- **2026-08-31** — Per [#449](https://github.com/msdixon/secret-cabinet/issues/449)'s 2026-08-28 decision, spiked the trigger signal before spending on generation: a new `reaction` field (happy/thinking/angry/none) on `pipeline-disposition.js`'s `update_disposition` tool call, verified against live model calls to self-report reliably (5/6 hand-authored cases matched exactly). With the spike passing, pilot-generated the [#450](https://github.com/msdixon/secret-cabinet/issues/450) reaction portrait set for 3 members (Crowley, Yeats, Teresa) through the existing #435 Gemini pipeline, verified live that the reaction tag correctly resolves to the generated image, and promoted all 10 candidates (including a regenerated Crowley baseline for robe/palette consistency) to `public/portraits/` after Rachel's review. Full-roster batch generation and the client-side trigger-to-display wiring (#452) are deliberate follow-ups, not done here.
- **2026-08-31** — `POST /api/members` now drafts the decided reaction set (happy/thinking/angry) alongside a new member's baseline portrait, anchored on the baseline via a reference image and gated behind the same `scripts/promote-portrait.js` human-review step — completing #450 alongside the existing-roster backfill (#470, #472, #484). See [#491](https://github.com/msdixon/secret-cabinet/pull/491).
- **2026-08-31** — `pipeline.js`'s four per-member cache-loader fallbacks, its metric-emission paths, and its "every speaker failed" throw were untested — coverage rose from 92.69%/67.92% (line/branch) to 100%/79.31%. A structural redundancy scan of `pipeline.test.js` found no significant duplicate tests, but did find 9 `runRound` integration tests re-declaring an identical args object; consolidated into one shared fixture, netting only +94 lines despite 8 new tests. [#445](https://github.com/msdixon/secret-cabinet/issues/445)
- **2026-08-31** — [#444](https://github.com/msdixon/secret-cabinet/issues/444): catch-block and metrics-callback branches were consistently uncovered across `src/routes/session.js`, `convene.js`, and `library.js` — happy paths were well tested, failure paths mostly weren't. Added one targeted test per gap; `convene.js` and `library.js` are now 100% line coverage, `session.js` up to 95.29% (the remaining gaps are ordinary untested branches, not error paths). [PR #459](https://github.com/msdixon/secret-cabinet/pull/459)
- **2026-08-31** — [#443](https://github.com/msdixon/secret-cabinet/issues/443): `src/routes/upload.js`'s `pdf2json` integration and multer error branches had zero test coverage (79.10% line / 83.33% branch). Added tests mocking `multer`/`pdf2json` via the require-cache pattern (same as `export-routes.test.js`'s `execFile` mock), bringing the file to 100% line / 95.83% branch coverage. [PR #462](https://github.com/msdixon/secret-cabinet/pull/462)
- **2026-08-31** — [#442](https://github.com/msdixon/secret-cabinet/issues/442): `src/routes/export.js` was the worst-covered file in the suite (75.63% line / 46% branch) — the whole `POST /api/dayone/entries` success path and most of `POST /api/ulysses/export` had zero coverage. Added tests for both, plus the remaining error `catch` blocks, bringing the file to 100% line / 81.08% branch coverage. [PR #448](https://github.com/msdixon/secret-cabinet/pull/448)
- **2026-08-31** — [#437](https://github.com/msdixon/secret-cabinet/issues/437): `GET /api/admin/visits` now splits its report into Traffic (page views — `GET /`, `/lodge`, `/reading-room/:id`) as the headline number and API calls as a clearly secondary section, derived from the existing `store.byRoute` data at render time rather than a new counting mechanism — a single page load fans out into several countable API calls on boot, so the old blended `total` massively overcounted actual visitors. [PR #438](https://github.com/msdixon/secret-cabinet/pull/438)
- **2026-08-31** — [#436](https://github.com/msdixon/secret-cabinet/issues/436) fixed: the "Add Member" character-file drafting call now disables adaptive thinking (same fix #406/#414 applied to `pipeline-director.js`/`pipeline-disposition.js`), so it can no longer spend its whole 7000-token budget reasoning and emit no text block, and an empty/whitespace-only generation is now rejected as a failure rather than silently written to disk and pushed into the roster. Also closed the sibling gap #436 flagged in `src/citations.js`'s `groundAgainstLibraryText`. [PR #447](https://github.com/msdixon/secret-cabinet/pull/447)
- **2026-08-31** — [#435](https://github.com/msdixon/secret-cabinet/issues/435) closed the loop #259 stopped short of: `POST /api/members` now calls the Gemini API (`gemini-2.5-flash-image`) directly, server-side, generating a real portrait candidate at `public/portraits/candidates/<id>.png` when `GEMINI_API_KEY` is set — no agent session needed, unlike the ElevenLabs MCP path that PR's scoping originally considered. Deliberately stops short of the canonical path: `node scripts/promote-portrait.js <id>` is the human-reviewed resize/place step, keeping `STYLE_GUIDE.md`'s validation requirement intact. Built after a live spike confirmed the mechanics for real (presigned-URL download, `sips` resize, `imageConfig.aspectRatio` orientation control) against actual paid test generations, not assumptions. [PR #433](https://github.com/msdixon/secret-cabinet/pull/433)
- **2026-08-31** — [#430](https://github.com/msdixon/secret-cabinet/issues/430): added `scripts/eval-citation-grounding.js` (`npm run eval-citation-grounding`), a golden-set accuracy harness for `groundAgainstLibraryText` — an 18-item curated set (real, inverted, and unsettled quotes across 6 library entries) scored against real model output, distinct from [#141](https://github.com/msdixon/secret-cabinet/issues/141)'s free scheduling simulation. Gated to `docs/MODEL-REVIEW.md`'s existing quarterly cadence (item 3 now points at it) since it makes a real, billed API call; supersedes and removes the ad hoc `scripts/test-library-grounding.js` (#153's original 3-case check against a stale model id).
- **2026-08-31** — Copy-only fix to the [#342](https://github.com/msdixon/secret-cabinet/issues/342) pre-convene stepper: the third panel (with the Convene button) had no title, unlike "I. The Provocation" and "II. The Assembled" above it — added "III. Convene" to match. Also renamed that panel's dossier-opening button from "The Assembled ↗" to "Review the assembly ↗" since it duplicated step II's title while opening something different. [#425](https://github.com/msdixon/secret-cabinet/issues/425), [PR #427](https://github.com/msdixon/secret-cabinet/pull/427).
- **2026-08-31** — Brightened the 3D room ([#424](https://github.com/msdixon/secret-cabinet/issues/424), [PR #426](https://github.com/msdixon/secret-cabinet/pull/426)): sconces went from 3 bare marker-sphere lights to 6 fixtures with a wall-mounted backplate + cup, flush-mounted via the existing `wallSpot()` convention like the rest of the #304 dressing rather than floating independently, at higher intensity/range so the light pools visibly overlap. Ambient `HemisphericLight` nudged 0.5 → 0.62 as a shadowless general fill, leaving the hearth and sconces as the room's real light sources untouched. Stretch goal shipped too: a static lit candle at the table's center, its own short-range `PointLight`, deliberately not tied to the hearth's `fireLevel`. Verified live via before/after screenshots.
- **2026-08-31** — Fixed the live-turn queue wedging forever after a mid-turn generation error ([#423](https://github.com/msdixon/secret-cabinet/issues/423)): a thrown mid-stream error left the in-flight turn stuck at the front of `witness.js`'s `#400` queue with no `block`, and `advanceLiveTurnQueue`'s guard then blocked everything behind it — including a successful retry — until a full `resetLiveTurnQueue()`. Confirmed via a jsdom reproduction against the real module, which also surfaced a compounding DOM collision in room mode. `witness.js` now exports `liveAbortTurn()`, called from `app.js`'s `abort()`, which evicts the stuck turn and cleans up its orphaned typing entry.
- **2026-08-31** — Visitation/traffic metrics for the public read tier (#379/#380), deliberately narrow: counts and logs unauthenticated requests to the app shell and public read-only API routes (`src/visits.js`), persisted to `visits.json` on `DATA_DIR` alongside a `GET /api/admin/visits` markdown summary behind the existing auth gate. No rate limiting, per-user auth, or alerting — just visibility, so the invite-only demo pool can be watched rather than assumed to stay small. [#422](https://github.com/msdixon/secret-cabinet/issues/422), [PR #428](https://github.com/msdixon/secret-cabinet/pull/428).
- **2026-08-31** — Fixed the camera swinging to later speakers while an earlier one was still visibly mid-turn ([#413](https://github.com/msdixon/secret-cabinet/issues/413)): `setSpeaking()` was called the instant each SSE `speaking` event arrived, which is raw generation pace and routinely outruns the paced, one-turn-at-a-time reveal `witness.js`'s `#400` live-turn queue built for exactly this reason — most visible on long, multi-beat turns, where generation for two or three speakers ahead can finish before the first one's beats are done being shown. Moved the camera call into `advanceLiveTurnQueue` itself, so framing now rides the same front-of-queue signal as the room cards and voice do, plus a matching reset when the queue drains or the stage resets. Verified live against a real multi-member, multi-beat convene.
- **2026-08-31** — Copy-only changes: the masthead's "The Lodge ↗" nav button now reads "Lodge Members ↗" (same length class as the sibling "Past Meetings" button), and the casting-suggestion label in step II of the convene flow changed from "Who would this draw?" to "Let the room choose". No interaction or styling touched. [#412](https://github.com/msdixon/secret-cabinet/issues/412) / [#418](https://github.com/msdixon/secret-cabinet/pull/418)
- **2026-08-31** — Added `scripts/rollup-metrics.js` (`npm run metrics-rollup`), a read-only aggregation of every session's `generationMetrics` into total/by-phase/by-date cost and volume, gitignored `METRICS-ROLLUP.md`. Flags when a scanned session likely predates [#225](https://github.com/msdixon/secret-cabinet/issues/225) or [#190](https://github.com/msdixon/secret-cabinet/issues/190)'s field additions, per `docs/MODEL-REVIEW.md` Step 1's own caveat, and notes it can't break totals out by model since no metric entry records which model generated it. [#409](https://github.com/msdixon/secret-cabinet/issues/409).
- **2026-08-31** — Wired `scripts/verify-library-sources.js` (the guardrail against fabricated `source_url`s, filed after #157) into CI: a `dorny/paths-filter` step gates it to only run on PRs touching `prompts/library/**`, and it's a hard gate (no `continue-on-error`) since the current 44 entries already pass clean. [#407](https://github.com/msdixon/secret-cabinet/issues/407) — went with option (a) from the issue (PR-time check) over (b) (weekly sweep), since it catches problems where the script is actually used today. The first CI run failed before any step of substance ran — `dorny/paths-filter` needs `pull-requests: read` on `GITHUB_TOKEN` to list a PR's changed files via the GitHub API, which the repo's default token permissions don't grant; added an explicit `permissions:` block to the job.
- **2026-08-31** — Switched the `MODEL` default in `server.js` to `claude-sonnet-5`, the follow-through on [MODEL-REVIEW.md](docs/MODEL-REVIEW.md)'s 2026-08-08 recommendation ([#216](https://github.com/msdixon/secret-cabinet/issues/216)). Added `thinking: {type: "disabled"}` to the two calls flagged as too tight for adaptive thinking's default budget (`callDirector`'s `select_speakers`, `callDispositionUpdate`). [#406](https://github.com/msdixon/secret-cabinet/issues/406) — voice-fidelity spot-check still outstanding, logged in the review table.
- **2026-08-31** — Bumped `jsdom` 29.1.1 → 30.0.1 (dev-only, `test/*.test.js`), the half of #251 split out as [#405](https://github.com/msdixon/secret-cabinet/issues/405) after Express 4→5 shipped separately under #136. Full suite (965 tests) passes unchanged; the suite has no `getComputedStyle`/CSS-cascade-adjacent assertions at all, so the risk category #251 flagged didn't actually apply here. One finding worth knowing: jsdom 30 declares `engines.node: "^22.22.2 || ^24.15.0 || >=26.0.0"`, which excludes Node 25.x (an odd, non-LTS release) — advisory `EBADENGINE` warning only, doesn't block install or tests, and CI's Node 22 (latest 22.23.2) is unaffected.
- **2026-08-31** — Deleted `server.js`'s two dead wrapper functions (`memberBrief`, `openSSE` — both zero-caller copies of code that moved elsewhere) and their now-unused `pipeline` destructures, plus five smaller unused-var lint findings across `lodge.js`, `sessions.js`, and `export.js`, and a `require-yield` fix in `pipeline.test.js`. `npm run lint` and `npm run format:check` are both clean for the first time since CI's report-only gate landed ([#277](https://github.com/msdixon/secret-cabinet/issues/277)) — [#404](https://github.com/msdixon/secret-cabinet/issues/404). Flipping `continue-on-error` off still needs a couple of consecutive clean CI runs first, per the issue.
- **2026-08-31** — [#370](https://github.com/msdixon/secret-cabinet/issues/370) wave 2's last open slot: a second Crowley entry, from "John St. John" in *The Equinox* Vol. I No. 1 (1909) — a comic, self-addressed diary passage from his 1908 magical retirement, sidestepping the same copyright concern that held both him and Yeats back originally (his autobiography's US renewal status is unresolved) by going pre-1923 instead, same as the Yeats entry ([PR #431](https://github.com/msdixon/secret-cabinet/pull/431)). Rachel supplied the source text; `source_url` was pointed at archive.org's own scan rather than the mirror she found it on, both for the stronger `/metadata` verification check and a cleaner citation trail. All 6-8 of wave 2's proposed second entries are now shipped (6 register-depth picks plus these 2 held-back slots), closing #370's own stated ceiling. [PR #432](https://github.com/msdixon/secret-cabinet/pull/432)
- **2026-08-31** — [#370](https://github.com/msdixon/secret-cabinet/issues/370) wave 2's remaining Yeats slot: a second entry from *The Trembling of the Veil* (1922), Book III "Hodos Camelionis" — the York Powell dinner-party thought-transmission anecdote, sourced and confirmed live with Rachel against the actual Gutenberg text after Yeats was originally held back for a copyright concern (letter collections under active estate control) that a pre-1923 published work sidesteps entirely. That live check also caught that secondary scholarship commonly misspells the chapter as "Hodos Chameliontos," corrected here and noted in the entry's `rights_note`. Crowley's slot remains open. [PR #431](https://github.com/msdixon/secret-cabinet/pull/431)
- **2026-08-31** — The mockup's "Reveal Player Turns ◆" button next to "Convene the Lodge" turned out to already exist verbatim — it's #31's post-lull transcript-highlight toggle, not a pre-convene control, and reusing its label would have collided with that working feature. The actual pre-convene player-participation control (the "Play as" details panel) was buried in II. The Assembled with no visibility from III. Convene; added a small live status button there instead, mirroring the current Play-as selection and opening that panel on click. [#348](https://github.com/msdixon/secret-cabinet/issues/348), [PR #440](https://github.com/msdixon/secret-cabinet/pull/440).
- **2026-08-31** — The `.after-panel` (Continue/Verify Citations/Watch/Metrics/Preserve) moved from stacked below the whole `.control-rail` to directly under `#stage-record`, and now toggles visible on every lull rather than only once at a meeting's true close — reconciled against Principle 6 and #245 by keying visibility to the room's own lull state instead of making the bar permanently visible. [#343](https://github.com/msdixon/secret-cabinet/issues/343), [PR #421](https://github.com/msdixon/secret-cabinet/pull/421).
- **2026-08-31** — Scoped the GitHub wiki's role — reviewer-primary (grant/PhD-portfolio), collaborator-secondary, no facts that can go stale silently, freshness-pass-before-external-use as the maintenance trigger — as a comment on [#306](https://github.com/msdixon/secret-cabinet/issues/306). Shipped four pages against that scoping: Home, Overview & Motivation, Architecture Overview, and Getting Started. Highlights and Project Vocabulary remain open, the former deliberately held for a separate curatorial pass.
- **2026-08-31** — [#259](https://github.com/msdixon/secret-cabinet/issues/259) scoped and given a first cut: the in-app "Add Member" flow now also drafts a `STYLE_GUIDE.md`-compliant portrait-generation prompt via the same Claude client already used for character-file drafting, appending it to a new `public/portraits/PENDING-PROMPTS.md`. An initial claim in this PR's own description that no image-gen tool was reachable from this environment was wrong and corrected — see [#435](https://github.com/msdixon/secret-cabinet/issues/435), scoped and built the same day in the same PR, for what that correction led to. [PR #433](https://github.com/msdixon/secret-cabinet/pull/433)
- **2026-08-31** — Shipped a first cut of [#196](https://github.com/msdixon/secret-cabinet/issues/196)'s splinter mechanic — two members trade a private aside while the main passage continues, generated on its own private context (not the shared `roundSoFar`) and reusing the existing #203 interrupt-intent signal as its trigger, then folded into the record as a diegetically bracketed block so every existing surface renders it correctly with no client change. Re-checked the #194 blocker first: its storage half was already resolved by #194's beat-level persistence, so this closes the remaining generation-side gap. Visual grammar (#184/#257's stage) and a director-initiated trigger are named as deliberate follow-ups, not gaps. [PR #456](https://github.com/msdixon/secret-cabinet/pull/456).
- **2026-08-31** — [#178](https://github.com/msdixon/secret-cabinet/issues/178) shipped: publishing a session to the reading room now opens a curation modal (a checkbox per passage, all checked by default) instead of publishing the whole transcript unconditionally, with a `publishedRounds` index array on the session record — absent/`null` still means every passage, so sessions published under #38's MVP render unchanged. An already-published session gets a new "Curate" action to revisit the selection without unpublishing, without bumping `publishedAt`. Adapted #178's original "rounds" proposal onto the #194 passage/lull data model — `session.rounds` never got renamed, so the index scheme reuses the same ordinal convention branching already keys off. [PR #441](https://github.com/msdixon/secret-cabinet/pull/441)
- **2026-08-31** — Shipped the first-cut evaluation harness scoped in the 2026-08-20 `/cabinet-review` comment on [#141](https://github.com/msdixon/secret-cabinet/issues/141): `scripts/eval-harness.js` (`npm run eval-harness`) reports per-meeting turn distribution and zero-turn shares from real sessions, beats-per-passage and `endedBy` breakdown, and a replayable seeded-RNG simulation of the real `pickNextSpeaker`/`isPoolExhausted` against current `tuning.js` constants — generalizing the inline harness #352/#353 built in `test/pipeline.test.js` into something re-runnable after a tuning change, with no API calls spent. Verified against current post-#352/#353 behavior (~0% zero-turn share, reconsult rate inside the 5%-95% band), not the issue's own pre-fix numbers. [PR #419](https://github.com/msdixon/secret-cabinet/pull/419).
- **2026-08-31** — First cut of [#34](https://github.com/msdixon/secret-cabinet/issues/34), "the document as object": a static open-book model on the table (hidden until a provocation is set), click-to-inspect camera framing plus a DOM reading panel for the text, and a page-glow tied to `setSpeaking()` standing in for "members gesture toward it" (no character rig exists to animate). Deliberately scoped like #357/#304's own first passes — per-passage highlighting synced to #355's citation capture is left for a later pass, not built here. [PR #455](https://github.com/msdixon/secret-cabinet/pull/455).
- **2026-08-31** — Follow-up to [PR #455](https://github.com/msdixon/secret-cabinet/pull/455), which scoped the deferred "per-passage highlighting synced to citations" half of #34 out of its first cut: a new `onCitation` SSE event relays each beat's live-captured citations (#355) to the client, and scene.js's `citeFromBeat()` highlights the matched passage — in the DOM reading panel if open, otherwise a distinct book-glow pulse — only when a citation actually quotes the active provocation document rather than some other real work. [#34](https://github.com/msdixon/secret-cabinet/issues/34), [PR #465](https://github.com/msdixon/secret-cabinet/pull/465).
- **2026-08-24** — [#400](https://github.com/msdixon/secret-cabinet/issues/400) fixed the live-convene half of #336 that PR #341 never actually touched. A first pass just serialized `Voice.speak()` calls in `public/js/witness.js`, which stopped audio cutting itself off but — caught in real live testing — left the *visual* side (typing indicators, settled cards) advancing unpaced, so the room raced far ahead of what was actually playing (a member's next turn, or a different member's, already on screen before the previous line's audio had even started). Replaced #279's per-member, WPM-only room hold with a single global live turn queue: `liveTypingStart`/`liveTypingSet`/`liveSpeech` now push onto one FIFO where only the front turn is ever rendered, paced by real `Voice.speak()` duration (WPM fallback when voice is off), extended to stage mode (previously entirely unpaced) and made lossless (#279 used to silently drop a turn superseded mid-hold). Separately, `speakViaElevenLabs` (`public/js/voice.js`) never applied `speedMultiplier` to the `<audio>` element's `playbackRate`, so the witness-speed control only ever changed text pacing, never the ElevenLabs audio most sessions actually hear — now set and clamped the same way the Web Speech path's rate is. Verified against real ElevenLabs synthesis in a live multi-member convene (not mocked), per #341's own admitted gap — confirmed via instrumented timing that card settling now tracks audio start/end instead of racing ahead, and that a mid-session speed change reaches the next beat. 949 total tests. [PR #402](https://github.com/msdixon/secret-cabinet/pull/402)
- **2026-08-24** — [#370](https://github.com/msdixon/secret-cabinet/issues/370) shipped 35a's first depth-after-coverage round: a second, different-genre authored entry for 6 of the library's 38 covered members, on axis A (register depth) per Rachel's pick over the issue's other three candidate axes. Each pairs the member's existing formal/published text with a conversational one — a letter, diary, or epistle — chosen where a public-domain source was easy to find and independently verify, several deliberately landing on themes the original entry already used for graph connective tissue (axis B as tiebreaker): William Blake's 1803 Felpham letter to Butts (echoes his own "contraries" and Catherine Blake's "felpham"/"correspondence"); John Dee's 1596 diary entry, a book-vision dream (echoes his Monas Hieroglyphica's "hieroglyph"); Teresa of Ávila's 1576 letter to her brother, funny and domestic against The Interior Castle's mysticism; Swedenborg's Journal of Dreams (raw dream-record against Heaven and Hell's polished empirical testimony); Jacob Böhme's Third Epistle, autobiographical about writing Aurora under persecution, against Signatura Rerum's cosmology; and H. P. Blavatsky's first letter to Sinnett, unguarded and funny against Isis Unveiled's treatise voice. Held for Rachel rather than curated this round: Crowley and Waite (autobiographies with unresolved US copyright-renewal status), Yeats and Jung (letter collections still under active copyright/estate control), and Ibn Khaldun (no public-domain English translation of his autobiography found).

A second author-credited entry surfaced a real gap: #187's `loadVoiceExemplar` only ever reads the *first* author match in `library.json`, so a second entry did nothing for the exemplar system it was meant to serve without a code change. Added `loadSecondaryVoiceExemplars` (`src/library.js`) and extended `pipeline-speaker.js`'s prompt builder to inject the primary exemplar at full weight plus any secondary entries as smaller, explicitly-supplementary "tone-tuning" passages — weighted lighter, correcting or sharpening the primary rather than competing with it. `test/library.test.js`'s old one-entry-per-author guard relaxed to a two-entries-per-author ceiling. 44 library entries, 6 with a secondary voice register, 965 total tests. [PR #403](https://github.com/msdixon/secret-cabinet/pull/403)
- **2026-08-24** — [#346](https://github.com/msdixon/secret-cabinet/issues/346) turned out mostly already shipped: the "Filter members…" input (2026-07-09) and the "who would this draw?" suggestion (#185's casting mechanism, 2026-08-07) predate the issue itself. Only the dashed-border restyle of "+ Invite to the Lodge" was genuinely open — `.add-member-btn` now matches the existing `.add-tag-btn` dashed pattern, visually distinct from seated-member chips' solid border — plus a placeholder copy tighten to match. [PR #397](https://github.com/msdixon/secret-cabinet/pull/397)
- **2026-08-24** — Preserve's export menu is now a row of small destination icons (txt / scholarly note / Day One / Ulysses / Obsidian, plus the non-local `.md` fallback) — selecting one shows only that destination's focused config, replacing the old always-all-fields-at-once list and the separate Export Settings drawer it opened into. Presentation only: the underlying export calls, the #37 scholarly gating, and the #331 citation-unlock logic are untouched. [#345](https://github.com/msdixon/secret-cabinet/issues/345)
- **2026-08-24** — [#344](https://github.com/msdixon/secret-cabinet/issues/344) shipped: Past Meetings' dense list rows are now cards in a CSS grid — a 2-4 overlapping portrait avatar-stack, date, and the existing italic one-line excerpt up front, with the six secondary actions (Watch/Reconvene/Thread/Compare/Metrics/Delete) collapsed behind a "more actions" toggle so the glance view stays compact; the publish-privacy disclosure stays unconditionally visible per #38's prior decision. `GET /api/sessions` now returns `memberIds` alongside `members` (index-aligned) so the client can resolve portrait URLs without a second round-trip. [PR #399](https://github.com/msdixon/secret-cabinet/pull/399)
- **2026-08-24** — Pre-convene flow: dismissible preamble banner + a numbered 3-step stepper (Bring a provocation → Choose who's in the room → Convene) with live checkmark/active/pending states, reconciled against #358's shipped room-above-controls layout rather than ported from the original mockup. The mockup drew a full-screen wizard header for real estate that no longer exists above the fold; instead the stepper is a compact wayfinding strip placed above `.control-rail` (not inside it, so it stays legible through the `#379` sign-in gate rather than dimming with the controls), with each step reading its own completion state independently since bringing a provocation and casting the room can happen in either order. See [#342](https://github.com/msdixon/secret-cabinet/issues/342).
- **2026-08-21** — [#350](https://github.com/msdixon/secret-cabinet/issues/350) accent half of #338's split shipped: a hand-set `voiceAccent` layers on top of gender/demeanor, filtered gender → accent → demeanor (accent wins the fallback tiebreak — a wrong accent is more jarring than a slightly-off demeanor). The premade 21-voice pool turned out to be 16 american/4 british/1 australian with zero German/French/Italian/Irish/Russian/Spanish coverage (verified live against `GET /v1/voices`), so `FALLBACK_VOICE_IDS` grew by 14 voices pulled from ElevenLabs' broader shared-voice library, each verified live via `/v1/text-to-speech`. 29 of 38 members tagged where the record is clear; 9 left unset (medieval Arab/Andalusian/Sufi figures, mixed-heritage cases) rather than guessed. Crowley now resolves to a British voice instead of the generically American one that motivated the issue — confirmed live end-to-end, not just tagged. Web Speech stays deliberately accent-unaware: no browser locale exists for "English spoken with a German/French accent," and forcing a non-English-locale voice would mispronounce the text outright. [PR #396](https://github.com/msdixon/secret-cabinet/pull/396)
- **2026-08-21** — [#380](https://github.com/msdixon/secret-cabinet/issues/380) shipped Option 2: `POST /api/voice/speak` is now public (`src/auth.js`), but `routes/voice.js` itself refuses to synthesize for an unauthenticated request — it serves a cache hit if one exists, or the same 503 an unconfigured server returns, which `public/js/voice.js` already falls through to Web Speech on. An authenticated request keeps today's behavior and may synthesize freshly, so watching a meeting once with voice on before publishing is what makes it sound right for visitors afterward, for free. Verified live: an unauthenticated request for a cached line served the real clip byte-for-byte with no ElevenLabs call, and one for an uncached line 503'd immediately with zero new synthesis. 939 tests. [PR #395](https://github.com/msdixon/secret-cabinet/pull/395)
- **2026-08-21** — [#379](https://github.com/msdixon/secret-cabinet/issues/379) shipped: an unauthenticated visitor now lands on the room itself, with published meetings reachable via `?session=<id>`, while convening stays behind the passphrase. `src/auth.js`'s `createRequireAuth` grows from three ad-hoc path bypasses into a generalized public/gated split — any non-API GET is public (the app shell), plus a fixed method+path allowlist for read-only API routes including the four `#378`-scoped session routes, now reachable in production for the first time. The control-rail renders `inert` under a sign-in overlay rather than hidden, per Principles 4 and 6. Verified live as both an unauthenticated and authenticated visitor, including a published/unpublished session pair confirming the exposure boundary holds. 924 tests. [PR #390](https://github.com/msdixon/secret-cabinet/pull/390)
- **2026-08-21** — A member can now genuinely decline a turn — `isPassTurn` detects the room's own action-only idiom as a deliberate pass, recorded as a distinct `passed: true` beat (never conflated with a failed call) and fed back into scheduling via partial ledger credit and a budget floor, per [#362](https://github.com/msdixon/secret-cabinet/issues/362). Also fixes a client-side gap the issue surfaced: `addSpeech` rendered any all-action turn with no speaker attribution, which would have made a deliberate pass indistinguishable from anonymous stage business ([#394](https://github.com/msdixon/secret-cabinet/pull/394)).
- **2026-08-21** — [#360](https://github.com/msdixon/secret-cabinet/issues/360) shipped: portraits now show four states instead of two (speaking vs. not) — `waitingOnMemberId` (#203's per-beat disposition signal) and the director's candidate pool were already computed server-side and discarded; two new SSE fields (`pool`, `disposition`) surface them, and `applySeatState` grows from 3 states to 6 (`listening`/`thinking`/`waiting` added), table-driven and eased via `BABYLON.Animation` instead of a flicker-prone direct mutation. Verified live via direct pixel readback of the Babylon scene's seat materials, not screenshots. [PR #391](https://github.com/msdixon/secret-cabinet/pull/391)
- **2026-08-21** — Re-synced the [Secret Cabinet Design System](https://claude.ai/design/p/9bddf4be-045e-4ea3-ad4e-18d24951cbda) canvas project's recreated main-salon screen against current production, including two rounds of self-caught correction beyond the issue's own named changes (#194, #257): a first pass missed #266's Cinzel identity-name typeface (now `--font-identity`, plus a corrected guideline card) and, more substantially, #358's room-first layout inversion (a first rebuild had kept the old two-column sidebar production explicitly tried and reverted). Also cleared several stale-copy leftovers ("three rounds" body text, two "I. The Document" labels) and fixed the same stale label/path in this repo's imported [layout.card.html](https://github.com/msdixon/secret-cabinet/blob/main/components/layout/layout.card.html) specimen. `github.md` now recommends walking `git log` on the real production files for the next re-sync, rather than trusting an issue body's own summary of what changed. [#349](https://github.com/msdixon/secret-cabinet/issues/349)
- **2026-08-21** — Scoped the public read tier Rachel asked for — the app readable without login so it can be linked to friends remotely, with convening still gated ([#378](https://github.com/msdixon/secret-cabinet/issues/378), [#379](https://github.com/msdixon/secret-cabinet/issues/379), [#380](https://github.com/msdixon/secret-cabinet/issues/380)); a stranger lands on the room itself rather than a gallery, her call. The scoping's main finding was that the intuitive rule — "anything that doesn't hit an API is safe for a visitor" — covers cost and misses exposure: `GET /api/sessions` costs nothing and returns every stored session with journal-entry previews and full-text search, unfiltered by `published`. Promoted **Security to Principle 7** in `docs/PRINCIPLES.md` on its own stated trigger, which required the review to happen before the app is shown outside the one-person context rather than after.
- **2026-08-21** — Scoped `GET /api/sessions`, `GET /api/threads`, `GET /api/sessions/:id`, and `GET /api/sessions/:id/transcript` by `published`: an unauthenticated caller now only sees published sessions (404 not 403 on the by-id routes, published-before-`?q=` on the list route), gated by a new `req.authed` set once in `auth.js` rather than re-derived per route. Prerequisite groundwork for [#379](https://github.com/msdixon/secret-cabinet/issues/379)'s public read tier — a behavioral no-op today since the passphrase gate still blocks unauthenticated `/api/` traffic before these handlers run. [#378](https://github.com/msdixon/secret-cabinet/issues/378), [PR #387](https://github.com/msdixon/secret-cabinet/pull/387)
- **2026-08-21** — [#374](https://github.com/msdixon/secret-cabinet/issues/374) fixed all three: member cards now render name/portrait/relationships/library-texts immediately from data already loaded at boot, with only the dossier's bio/voice prose fetching in afterward instead of gating the whole card — the actual bio cutoff turned out to be `extractSection()` defaulting to a 320-char slice with no override on the dossier route, now unlimited by default. "Reheat ⟲" is renamed "Reset Layout ⟲", function unchanged. [PR #385](https://github.com/msdixon/secret-cabinet/pull/385)
- **2026-08-21** — [#373](https://github.com/msdixon/secret-cabinet/issues/373) fixed: `stripForSpeech()` in `public/js/voice.js` stripped every `*...*`-wrapped span before speaking, so inline italic keyword text ("stabilitas loci", "'asabiyya'") vanished from narration exactly like real stage directions, silently dropping content meant to invite interaction. Now only a whole line that is nothing but one asterisk span is dropped (mirroring `app.js`'s `renderActions()` block/inline split); other inline italics keep their words, asterisks stripped, and get their own `.keyword-inline` style distinct from `.action-line`. Whether action text belongs outside the dialogue card (#291) and whether a third distinct text style is warranted both stay open for a future pass — no syntax currently distinguishes a genuine mid-sentence action aside from inline keyword text, so a mid-sentence aside will now be spoken too. 826 tests (2 updated, 2 added). [PR #377](https://github.com/msdixon/secret-cabinet/pull/377)
- **2026-08-21** — [#364](https://github.com/msdixon/secret-cabinet/issues/364): the pacing/scheduling constants scattered across `pipeline.js`, `pipeline-speaker.js`, and `lodge-prompts.js` — each overdue for the 2026-08-19 calibration review that never had a single place to happen — moved into a new `src/tuning.js`, values and rationale comments intact, logic untouched. Pure move: 824 tests unchanged. [PR #375](https://github.com/msdixon/secret-cabinet/pull/375)
- **2026-08-21** — [#361](https://github.com/msdixon/secret-cabinet/issues/361) metrics panel now renders a "0 calls, sat silent" row for every seated member with no `speaker`/`disposition` metric, instead of omitting them — the verification instrument for #352/#353's turn-distribution fixes. [PR #386](https://github.com/msdixon/secret-cabinet/pull/386)
- **2026-08-21** — [#359](https://github.com/msdixon/secret-cabinet/issues/359) fixed: post-#358, the room renders above the `.control-rail` controls, so clicking Convene grew `#stage-pane` to 75vh but never moved the page — the live stage started off-screen above the still-visible controls. `liveReset()` now scrolls `#stage-pane` into view the same way the Witness replay path already did, via a shared `scrollToStage()` helper that respects `prefers-reduced-motion`. [PR #384](https://github.com/msdixon/secret-cabinet/pull/384)
- **2026-08-21** — [#358](https://github.com/msdixon/secret-cabinet/issues/358) shipped: the room now renders first and is the resting default (big, record hidden — measured live going from starting at y=1339 to y=103 at 1280x720, fully above the fold), with provocation/casting/convene in a `.control-rail` below it instead of stacked full-width above. The masthead condensed to a slim identity bar, which resolves [#303](https://github.com/msdixon/secret-cabinet/issues/303) as a side effect — no separate answer needed. A narrower side-by-side control layout was tried and reverted after live verification showed the up-to-50-member roster wrapping into an unreasonably tall column; mobile got its own `min(vh, vw*ratio)` fix so the canvas stays letterboxed rather than turning portrait-shaped. [PR #383](https://github.com/msdixon/secret-cabinet/pull/383)
- **2026-08-21** — [#356](https://github.com/msdixon/secret-cabinet/issues/356) shipped: a two-scope appendix-style bibliography — Works Cited, Works Referenced (invoked-not-quoted, a new weaker tier piggybacked on #355's per-beat capture), and the curated library as its own appendix — deliberately reshaped from `build-citation-manifest.js`'s review-oriented, verdict-severity-ordered `CITATION-MANIFEST.md`. Fixes the concrete bug PROJECT.md flagged: the scholarly export's Bibliography section only ever read `citationFlags`, so it rendered empty for 10 of 11 local sessions that had never been run through Verify Citations; it now falls back to the always-on beat capture. Also fixed a real, independent bug found along the way: `src/library.js` wasn't decoding backslash-escaped quotes in a library entry's `citation` frontmatter string, corrupting nearly a third of the new Library appendix's entries. [PR #389](https://github.com/msdixon/secret-cabinet/pull/389)
- **2026-08-21** — [#355](https://github.com/msdixon/secret-cabinet/issues/355): citation extraction is now always-on, piggybacked per beat on the existing disposition call (bounded to that turn's text, `memberId` already known) rather than a single opt-in whole-transcript pass — 1 of 11 local sessions had ever been run through Verify Citations before this. `beats[].citations` accumulates on the structured record; `/verify-citations` now only runs the deliberate library/web grounding pass against that capture, and `build-citation-manifest.js` includes every session's citations, grounded or not, rather than only the ones someone remembered to verify. [PR #382](https://github.com/msdixon/secret-cabinet/pull/382)
- **2026-08-21** — [#353](https://github.com/msdixon/secret-cabinet/issues/353) fixed: the mid-passage director re-consult fired in 0.0% of a 20,000-passage simulation because both its triggers assumed beat counts (10-14) real ~4.3-beat passages never reach. Candidate pools are now sized against a realistic 220-word/beat estimate (matching [#352](https://github.com/msdixon/secret-cabinet/issues/352)'s own simulation figure) instead of a 150-word placeholder, `POOL_SLACK` dropped 2 → 1, and the unreachable beat-count trigger is replaced with one keyed off words spent since the last consult — reachable in ~29% of a re-run of the same simulation without becoming the norm. [PR #376](https://github.com/msdixon/secret-cabinet/pull/376)
- **2026-08-20** — State-of-the-app review (`/cabinet-review`), the first pass grounded in simulating the app's own scheduler rather than reading it: against the real `pickNextSpeaker`, the bottom-ranked member of a candidate pool is silent in **71%** of passages, and the mid-passage director re-consult meant to catch that fires in **0.0%** of them (its thresholds need ~10-14 beats; passages run ~4.3). Filed 13 issues across three arcs — the record and the ledger ([#352](https://github.com/msdixon/secret-cabinet/issues/352)-[#356](https://github.com/msdixon/secret-cabinet/issues/356), [#361](https://github.com/msdixon/secret-cabinet/issues/361), [#362](https://github.com/msdixon/secret-cabinet/issues/362)), the room earning the fold ([#357](https://github.com/msdixon/secret-cabinet/issues/357)-[#360](https://github.com/msdixon/secret-cabinet/issues/360)), and calibration ([#363](https://github.com/msdixon/secret-cabinet/issues/363), [#364](https://github.com/msdixon/secret-cabinet/issues/364)) — plus Rachel's own addition that the app cannot yet produce a reliable bibliographic record of itself (1 of 11 sessions had ever been citation-verified, yielding 0 works cited). [#17](https://github.com/msdixon/secret-cabinet/issues/17) ambient audio closed as not planned at her decision; #304 corrected to Done; `docs/PRINCIPLES.md` Principle 6 widened to govern spatial hierarchy rather than only controls.
- **2026-08-20** — [#363](https://github.com/msdixon/secret-cabinet/issues/363) closed: `lodge-context.md`'s two round-structure passages rewritten in the passage/lull vocabulary #194's migration actually shipped — this file is the cached system prefix on every director/speaker call, so the model had been reading a description of a meeting shape #244/#245/#246 retired months ago. Also corrected `pipeline-speaker.js`'s exemplar-coverage comment to the real 36-of-38 number (gap is Corbin and Sun Ra only) and removed the dead `roundCount` field from new sessions and branches. [PR #366](https://github.com/msdixon/secret-cabinet/pull/366)
- **2026-08-20** — [#357](https://github.com/msdixon/secret-cabinet/issues/357) the room's most-invoked object — the fire, named ~14 times across the runtime prose — now has real fireplace geometry (surround, firebox, five animated flame cones, ember bed) instead of a single 0.4-diameter sphere, and reads the meeting: `LodgeScene.setPassageCount()`/`stirFire()`, wired through `app.js`'s `addLullDivider`, bank it down as passages accumulate and flare it on a "stirs the fire" lull note. Fixed live: a solid chimney-breast box was fully occluding the firebox behind it, same problem `buildBookshelves`' own comment warns about for a solid carcass. Gates [#358](https://github.com/msdixon/secret-cabinet/issues/358)'s room-above-controls layout. [PR #371](https://github.com/msdixon/secret-cabinet/pull/371)
- **2026-08-20** — [#354](https://github.com/msdixon/secret-cabinet/issues/354): closed the four holes in `session.rounds[].beats`, the structured turn-level record #244 introduced — interjections now push a real segment instead of appending prose, failed speaker turns are recorded with an explicit `failed` marker instead of dropped, and the player's own turn carries a stable id instead of `null`. Pre-#244 sessions are left alone but now legible as incomplete (a new `recordCompleteness()` check surfaces in the scholarly export as a caveat). New `public/js/record.js` centralizes the segment-kind/label-placement vocabulary this touches. 794 tests. [PR #368](https://github.com/msdixon/secret-cabinet/pull/368)
- **2026-08-20** — [#352](https://github.com/msdixon/secret-cabinet/issues/352) meeting-level turn ledger shipped: `turnsSoFar(rounds)` (a who-has-spoken sibling to `wordsSpentSoFar`) is now threaded into `pickNextSpeaker`'s draw as an under-heard boost and into the director prompt as an explicit list of who hasn't spoken tonight. A distribution test reproduces the issue's own simulation and confirms the whole-meeting-silent rate drops >5x without flattening the director's ranking. Undercounts interjection turns until [#354](https://github.com/msdixon/secret-cabinet/issues/354) lands — the safe-direction limitation, called out inline. [PR #367](https://github.com/msdixon/secret-cabinet/pull/367)
- **2026-08-20** — [#338](https://github.com/msdixon/secret-cabinet/issues/338) demeanor half shipped: a hand-set `voiceDemeanor` (grounded/stately/intense) per roster member layers on top of #333's gender filter in both TTS backends — `assignVoiceId` narrows by demeanor after gender (ElevenLabs), and `voice.js` biases the deterministic per-member pitch/rate since browsers expose no per-voice demeanor signal to filter against (Web Speech). All 38 members' `voiceId` re-derived so the assignment is actually live. Accent split out to [#350](https://github.com/msdixon/secret-cabinet/issues/350) — needs a live ElevenLabs voice-library check this environment couldn't run. [PR #340](https://github.com/msdixon/secret-cabinet/pull/340)
- **2026-08-20** — [#336](https://github.com/msdixon/secret-cabinet/issues/336): Witness auto-advance paced every beat off a fixed WPM guess even with voice on, cutting a long line off mid-sentence when the real utterance ran slower than the guess. `Voice.speak()` (`public/js/voice.js`) now returns a promise that resolves on the browser's actual `end`/`ended` completion event, and replay's `advance()` (`public/js/witness.js`) awaits it when something was actually spoken, falling back to the WPM estimate otherwise — a generation counter guards against a stale, late-resolving promise scheduling a phantom advance after the user goes back. 3 new tests, 752 total. [PR #341](https://github.com/msdixon/secret-cabinet/pull/341)
- **2026-08-20** — [#333](https://github.com/msdixon/secret-cabinet/issues/333): both #29 TTS backends picked a member's voice by hashing their id into a flat pool with no relationship to who the member is — now filtered by a hand-set `voiceGender` field per roster member before the same deterministic hash, in `src/roster.js`'s `assignVoiceId` (ElevenLabs) and `public/js/voice.js`'s `voiceForMember` (Web Speech, via a name-based gender heuristic over the browser's own voice list). Closes the "hand-authored voice field" item #29's remaining-work note left open; the note's other item (auto-advance cutting audio off mid-sentence) is split out as [#336](https://github.com/msdixon/secret-cabinet/issues/336), previously untracked. 746 tests. [PR #337](https://github.com/msdixon/secret-cabinet/pull/337)
- **2026-08-20** — [#331](https://github.com/msdixon/secret-cabinet/issues/331): the Scholarly Note export unlocked only on annotating a passage — a hidden click-to-reveal interaction with no visible affordance — while verifying citations, the instinctive first move for a citation-framed export, did nothing. Either signal now unlocks it, `exportScholarly()` can produce a bibliography-only note, and the button's title explains both paths. 739 tests. [PR #334](https://github.com/msdixon/secret-cabinet/pull/334)
- **2026-08-20** — #330: `pickNextSpeaker`'s weighted draw now reads the director's priority order instead of discarding it — the pool from `selectSpeakers` was always ordered by relevance, but every candidate had equal odds regardless, so an obviously-relevant member (e.g. William Blake for a direct question) could go a whole passage without a turn. Each rank step down from the top now discounts draw weight (~2.4x top-vs-bottom in a 5-seat pool), stacking with the existing tendency/repeat/interrupt factors as one more weight, not a forced pick. [PR #335](https://github.com/msdixon/secret-cabinet/pull/335)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — ninth in the 2026-08-18 35a pickup order, after Adorno, Pauli, Yates, Scholem, Catherine Blake, Warburg, Bamba, and Frieda Harris: an Abraham Abulafia entry. This track was explicitly paused for autonomous pickup because it needed a human-confirmed source; Rachel supplied one — Moshe Idel, "Abraham Abulafia on the Messiah and the Pope," *Religions* 16, no. 3 (2025): 273, an open-access CC BY 4.0 article quoting Idel's own translations of Abulafia's Hebrew. Combines two short passages on the divine name as the point of interfaith convergence: the 1274 opening poem of *Mafteaḥ ha-Ra'ayon* and the 1280 *Sitrei Torah* passage on Jews, Christians, and Ishmaelites all pointing to the same "essence of unicity." First `cc-by-4.0` entry in the library rather than `fair-use` — the article itself, not just the underlying primary source, carries the open license, so the translation is reusable in full rather than fair-use-sized. Coverage: 35 → 36 of 38 by author. 762 tests, unchanged. [PR #351](https://github.com/msdixon/secret-cabinet/pull/351)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — seventh in the 2026-08-18 35a pickup order, after Adorno, Pauli, Yates, Scholem, Catherine Blake, and Warburg: an Amadou Bamba entry. This one was on the original hard-pass list because Bamba wrote almost entirely in Arabic verse and prose, so a usable entry needed an actual English translation, not a paraphrase — that's exactly what Rachel supplied: a PDF of Thomas Woerner-Powell's open-access chapter "Hierarchy, Community, and Nonviolence in Senegal: Amadou Bamba," in *Pacifism and Non-Violence in Contemporary Islamic Philosophy* (Cambridge University Press, 2025), which quotes Bamba's *Masālik al-Jinān* ("Ways unto Heaven," trans. Abdoul Aziz Mbacké, Lahore: Majalis, 2009) throughout. Of several candidate quotations in the chapter, picked the single most self-contained one with unambiguous sourcing (one work, one page, first-person) over two others Rachel first flagged — one sourced only to secondary hagiography, the other citing a work title inconsistent with its page citation. Excerpt (~95 words) is Bamba's "my two weapons are knowledge and worship" passage on spiritual vs. armed jihad. Filed `fair-use` with a `rights_note`, since the primary translated volume itself isn't independently verifiable online; `source_url` points to the chapter's DOI instead, checked against Crossref by `scripts/verify-library-sources.js` (34/34). Coverage: 33 → 34 of 38 by author, 34 → 35 by members-union. 739 tests, unchanged. [PR #332](https://github.com/msdixon/secret-cabinet/pull/332)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — fourth in the 2026-08-18 35a pickup order, after Pauli, Yates, and Scholem: a Catherine Blake entry. She was illiterate until William taught her; the only correspondence known going in was post-1827 estate letters (to Linnell, Tatham, Lord Egremont) whose accessibility couldn't be confirmed without library access. Rachel found something earlier and better on HathiTrust — three pages from *The Letters of William Blake* (ed. Russell, Methuen 1906) showing an unmentioned 14 September 1800 letter to Mrs. Anna Flaxman on the Blakes' move to Felpham, opening and closing in Catherine's own prose (with a William-authored song inset mid-letter), signed "CATHERINE BLAKE." Public domain — full letter reproduced, no fair-use sizing. `source_url` points to the archive.org mirror of the same edition rather than the HathiTrust catalog page, since the latter 403s to non-browser requests and would fail `verify-library-sources.js`. Coverage: 31 → 32 of 38 by author, 32 → 33 by members-union. 737 tests, unchanged. [PR #326](https://github.com/msdixon/secret-cabinet/pull/326)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — a Henry Corbin entry, the first 35a work outside the 2026-08-18 hard-pass cohort: he'd been covered only as a *subject* in Jung's 1916 entry, never in his own hand. Sourced live with Rachel — found and verified this session rather than a lead she'd already vetted, then confirmed with her before drafting per #316's human-in-the-loop protocol, since Corbin had reportedly turned out "less tractable than he looked" in a prior pass. Excerpt (~230 words) is from "Mundus Imaginalis: or The Imaginary and the Imaginal," trans. Ruth Horine, *Spring 1972* (Zürich: Spring Publications) — a version condensed with Corbin's own permission from the 1964 French original — the passage that defines "imaginal" against "imaginary" via *jism mithali*, the exact distinction his character prompt is built on. Public domain ruled out first (d. 1978, 1972 journal publication with identifiable translator/publisher); filed `fair-use` with a `rights_note`, same pattern as Adorno/Warburg/Bamba. Coverage: 36 → 37 of 38 by author — Sun Ra is now the only member left uncovered. 762 tests, unchanged. [PR #369](https://github.com/msdixon/secret-cabinet/pull/369)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — eighth in the 2026-08-18 35a pickup order, after Adorno, Pauli, Yates, Scholem, Catherine Blake, Warburg, and Bamba (#332): a Frieda Harris entry. This track was explicitly paused for autonomous pickup because it needed a human-confirmed source; Rachel supplied one — Deja Whitehouse, "'Mercury Is in a Very Ape-Like Mood': Frieda Harris's Perception of Thelema," *Aries* 21.1 (2021): 125–152, an open-access (CC BY 4.0) article quoting Harris's unpublished letters to Crowley. Harris has no standalone published prose of her own, so unlike prior entries this one combines three short quotes (11 Dec [1939], 22 Oct 1940, 8 Nov 1944, ~195 words total) into a single entry, the same multi-quote pattern the Scholem diaries entry used — chosen because together they show a more distinct voice than any one alone: wry domestic-magical friction, the "plumber-demons" anecdote that gives the article its title, and a closing alchemical self-assessment. Filed `fair-use` rather than `public-domain`: the letters are unpublished archival material (Syracuse SCRC / Warburg Institute Yorke Collection) still under her estate's copyright, reproduced here via Whitehouse's CC-BY article. Coverage: 33 → 34 of 38 by author. 739 tests, unchanged. [PR #339](https://github.com/msdixon/secret-cabinet/pull/339)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — second 35a increment sourced live with Rachel, next in the pickup order after Adorno ([PR #314](https://github.com/msdixon/secret-cabinet/pull/314)): a Pauli entry from his 1956 Preface to the English edition of *Theory of Relativity* (Dover 1981 repr. of the 1958 Pergamon ed.), on the æther becoming superfluous once relativity's group-theoretic structure did the explanatory work instead. Located via Domenico Giulini's 2008 paper on Pauli rather than the primary edition directly — `citation` records the "as quoted in" provenance trail rather than a new schema field. Fair-use tier, same as Adorno. `source_url` verified live against archive.org's `/metadata` API before committing. Coverage: 27 → 28 of 38 roster members by author. 694 tests, unchanged. [PR #318](https://github.com/msdixon/secret-cabinet/pull/318)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — fourth 35a increment sourced live with Rachel, next in the pickup order after Yates ([PR #322](https://github.com/msdixon/secret-cabinet/pull/322)): a Gershom Scholem entry, two diary passages on Torah-as-history and language as a "constant function" from *Lamentations of Youth: The Diaries of Gershom Scholem, 1913–1919* (trans. Anthony David Skinner, Harvard UP 2007), sourced via Paula Schwebel's open-access chapter "The Tradition in Ruins" (De Gruyter, 2018), which quotes both passages directly. `translated: true`, fair-use tier since Scholem died 1982 and the translation is still in copyright; `source_url` is Schwebel's chapter DOI, verified live against Crossref before committing. Coverage: 30 → 31 of 38 roster members. 713 tests, unchanged. [PR #325](https://github.com/msdixon/secret-cabinet/pull/325)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — tenth and last in the 2026-08-18 35a pickup order, after Pauli, Yates, Scholem, Catherine Blake, Adorno, Warburg, Bamba, Frieda Harris, and Abulafia: a Sun Ra entry. The pickup order flagged him as possibly needing purchase or special-collections access, and that held for the primary sources — both *The Immeasurable Equation* (Waitawhile, 2005) and *The Wisdom of Sun Ra* (WhiteWalls, 2006) are in-copyright and offline — but an open-access route existed: Nathaniel Earl Bowles, "'My Music is Words' — The Poetics of Sun Ra" (MA thesis, Virginia Tech, 2008), in VTechWorks, quotes both verbatim with page citations into each edition. Pairs poetry with prose at Rachel's direction — one voice in two registers: six of fourteen lines of "The Flesh" running the flesh/fresh, test-tester-testament cascade as verse, and the opening of the 1950s Chicago broadsheet "Jesus Said, 'Let the Negro Bury the Negro'" running the same letter-permutation mechanism as street polemic. Deliberately sized down to ~100 words across two works, neither reproduced whole: the estate is active, so the entry stays well inside fair use rather than at its edge. Interview material in the same thesis was excluded as transcription rather than his own hand. Filed `fair-use` with a `rights_note`; `translated: false`; `source_url` is the thesis's VTechWorks handle, since the Poetry Foundation's Sun Ra pages 403 to non-browser requests and would fail `verify-library-sources.js` — the same constraint that steered the Catherine Blake entry off HathiTrust. Coverage: 37 → 38 of 38 by author — with the Corbin entry ([PR #369](https://github.com/msdixon/secret-cabinet/pull/369)) landing first, this closes the last gap: every roster member now has an excerpt in their own hand, so every speaker prompt gets a voice-register exemplar (#187), and the 2026-08-18 hard-pass cohort is fully worked through. 794 tests, unchanged; 38/38 source_urls verified. [PR #372](https://github.com/msdixon/secret-cabinet/pull/372)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — sixth in the 2026-08-18 35a pickup order, after Adorno, Pauli, Yates, Scholem, and Catherine Blake: an Aby Warburg entry. This track was explicitly paused for autonomous pickup because it needed a human-confirmed source; Rachel supplied one — a 1938 PDF found via OpenAlex of "A Lecture on Serpent Ritual," trans. W. F. Mainland, *Journal of the Warburg and Courtauld Institutes* 2 (1938/1939): 277–292, republished by Universitätsbibliothek Heidelberg's ART-Dok repository. The lecture was delivered in German in 1923 and never published in Warburg's lifetime (he died 1929) — the German original is public domain, but this 1938/39 English translation is a separate copyrighted work, so the entry is filed `fair-use` with a `rights_note` rather than `public-domain`. Excerpt is the lecture's closing two paragraphs (~190 words of ~8,000) — "telegraph and telephone are destroying the cosmos," his best-known formulation of technology collapsing the symbolic distance myth had built. `source_url` points to the ART-Dok landing page rather than the `doi.org` link, since this is a DataCite DOI that Crossref (what `verify-library-sources.js` checks against) doesn't index. Coverage: 32 → 33 of 38 by author, 33 → 34 by members-union. 737 tests, unchanged. [PR #327](https://github.com/msdixon/secret-cabinet/pull/327)
- **2026-08-20** — [#316](https://github.com/msdixon/secret-cabinet/issues/316) — third 35a increment sourced live with Rachel, next in the pickup order after Pauli ([PR #318](https://github.com/msdixon/secret-cabinet/pull/318)): a Frances Yates entry from the Preface to *Giordano Bruno and the Hermetic Tradition* (1964), her own account of how the book's Hermetic-Bruno thesis came together, sourced from the publisher's free preview of the Routledge Classics 2002 edition (pp. xi-xii). Her own English (`translated: false`), fair-use tier since she died in 1981. `source_url` is the work's DOI, verified live against Crossref before committing. Coverage: 28 → 29 of 38 roster members. 694 tests, unchanged. [PR #322](https://github.com/msdixon/secret-cabinet/pull/322)
- **2026-08-20** — PROJECT.md's flat Todo-status backlog list moved to the same fragment pattern #309 gave STATUS.md: `docs/roadmap/todo/` holds one file per item, `scripts/assemble-todo.js` regenerates the list in place from whatever fragments exist (adapted for a list that shrinks as items ship, not just grows — deleting a fragment is the entire "mark done" action). PROJECT.md's board-authority language is now explicit — the GitHub Project board is authoritative for per-item status, PRs update it directly, and PROJECT.md's own Todo list/table only change via fragment assembly or the periodic weekly review, never inline per-PR — and CLAUDE.md's process rules (plus its still-pre-fragment STATUS.md description) were updated to match. [PR #313](https://github.com/msdixon/secret-cabinet/pull/313)
- **2026-08-20** — STATUS.md moved to a fragment-based workflow: PRs drop a file in `docs/status/fragments/` instead of editing STATUS.md directly, and `scripts/assemble-status.js` (manual, or weekly via `.github/workflows/assemble-status.yml`) folds pending fragments into dated entries — the same pattern Towncrier/Changesets use to stop parallel branches from conflicting at STATUS.md's single insertion point. `.gitattributes` adds `STATUS.md merge=union` as a transition-period stopgap. This entry is itself the first fragment assembled under the new convention. [PR #309](https://github.com/msdixon/secret-cabinet/pull/309)
- **2026-08-20** — A `ShadowGenerator` bound to the hearth `PointLight` (sconces stay shadowless fill light — a generator per sconce would triple the cost of the already-expensive point-light cube-map shadows for a room this small) now casts real shadows from the table and seats onto the floor, closing out #294's deliberately-deferred shadow work. Avatar billboards excluded as casters per the issue's own steer (a flat card would cast a card-shaped shadow reading as a bug, not atmosphere); verified live at 60fps with all 8 seats occupied, the worst-case caster count. [PR #320](https://github.com/msdixon/secret-cabinet/pull/320)
- **2026-08-20** — [#304](https://github.com/msdixon/secret-cabinet/issues/304): second art pass on the 3D lodge room, on top of #294's bare wall — baseboard/chair-rail/crown molding, 8 pilasters, 4 gilt portrait frames echoing the mantel's dorian-frame motif (#295), and 2 open-fronted bookshelves with visible books. First bookshelf version used a single solid carcass whose own front face occluded every shelf/book behind it (caught via a cropped/brightened render); fixed by splitting into a thin back panel + two end panels with an open front. All static geometry, no camera/behavior changes. 694 tests unchanged. [PR #321](https://github.com/msdixon/secret-cabinet/pull/321)
- **2026-08-20** — Added a playback speed control (0.75x/1x/1.5x/2x, persisted in `localStorage`) covering room-mode's #279 live-card hold, replay pacing, and the old stage's typing/reveal timing in one change — all three already funnel through `witnessReadingTime()`, so one multiplier at the end of that function reaches every surface. Per #288's 2026-08-17 decision comment, this is the broad-scope version, not just the narrow #279 gating. [PR #311](https://github.com/msdixon/secret-cabinet/pull/311)
- **2026-08-20** — A member's room card now holds a short-lived stack of recent beats instead of overwriting its content on every turn: each beat appends a `.room-card-entry`, older entries dim and scroll out of view (reusing #291's existing card scroll), and the whole stack still fades together on inactivity — implementing #287's 2026-08-17 decision (a per-member stack, not a separate history rail) as the deferred second half of #279. [PR #310](https://github.com/msdixon/secret-cabinet/pull/310)
- **2026-08-20** — #280 (direct-question entry) shipped as a pure copy/prompt change rather than a new entry-point mode — the pipeline already treated the entry field as arbitrary text and grounding was already independent of input type, so every place the app said "document" (UI, casting prompt, passage preamble, arc notes) now says "provocation" instead, a word that covers a bare question equally. Live-verified against a real question through the actual UI before merging. [PR #328](https://github.com/msdixon/secret-cabinet/pull/328)
- **2026-08-20** — #268's relationship-as-data layer shipped: `src/relationships.js` composes a per-speaker, per-evening fallback seeded from the existing knowledge graph (#22/#84) — reusing its edge shape directly (`type`→register, `label`→shared-history text) rather than a new schema — for any present-member pair a speaker's own character file doesn't already narrate in prose. Verified inert against the current 38-member cast (every seed edge already has prose coverage), activating only for future members or pairs the prose hasn't caught up to. [PR #312](https://github.com/msdixon/secret-cabinet/pull/312)
- **2026-08-20** — [#153](https://github.com/msdixon/secret-cabinet/issues/153) follow-up: today's scheduled manual-promotion check-in found `scripts/build-citation-manifest.js` never read `RAILWAY_VOLUME_MOUNT_PATH`, hardcoding `<repo-root>/sessions` instead of the volume-mounted dir `server.js` actually uses — a real shell on the live container would have scanned the wrong path, not just a sandbox with no session data. `loadSessions()` now takes `sessionsDir` explicitly and the CLI entry point resolves it the same way `server.js`'s `SESSIONS_DIR` does. New `GET /api/admin/citation-manifest` (`src/routes/session.js`) runs the same builder against the live `sessionsDir` and returns the markdown directly, gated behind the existing passphrase auth like every other `/api/*` route — the mechanism the stalled check-in needed to get real occurrence/web-hit data going forward instead of a 0-sessions result. 713 tests unchanged. [PR #323](https://github.com/msdixon/secret-cabinet/pull/323)
- **2026-08-20** — [#35](https://github.com/msdixon/secret-cabinet/issues/35) — first 35a increment sourced live with Rachel rather than via search-tool passes: an Adorno entry from his solely-authored "Types and Syndromes" chapter in *The Authoritarian Personality* (Verso 2019 ed.), and the first entry to actually use the `license`/`rights_note` fair-use tier #35 opened 2026-08-08 for the 10-member no-public-domain-text gap. `source_url` verified live against Verso's own product page before committing, per #157's discipline. Coverage: 26 → 27 of 38 roster members by author. 694 tests, unchanged. [PR #314](https://github.com/msdixon/secret-cabinet/pull/314)
- **2026-08-20** — [#35](https://github.com/msdixon/secret-cabinet/issues/35) — one library entry for **pixie** (Pamela Colman Smith), "Should the Art Student Think?" (*The Craftsman* 14, no. 4, July 1908), closing her author gap (previously a subject-only mention in Waite's 1911 preface entry, never author of her own prose). Straightforwardly public domain — the first paragraph of the article, transcribed directly from archive.org's `craftsman1419unse` OCR scan, not a fair-use excerpt. `source_url` verified live before committing, per #157. Coverage: 27 → 28 of 38 roster members by author. Requested explicitly by Rachel mid-session after #35a was otherwise paused for autonomous work — see the issue's 2026-08-18 scoping comments. 694 tests, unchanged. [PR #315](https://github.com/msdixon/secret-cabinet/pull/315)
- **2026-08-20** — [#29](https://github.com/msdixon/secret-cabinet/issues/29) second pass: an optional ElevenLabs backend for Witness voice playback, entirely gated behind `ELEVENLABS_API_KEY`. `src/routes/voice.js` proxies the TTS call and caches audio on disk keyed by voice+text so a replayed line is never re-synthesized or re-billed; `roster.js` backfills a deterministic `voiceId` per member the same hash scheme the Web Speech fallback already uses, but only once a key is configured, so an install with no key never gets `roster.json` touched. `voice.js` falls back to the untouched Web Speech path on any failure. Tested live against a real ElevenLabs account: the first version's `FALLBACK_VOICE_IDS` guess at ElevenLabs' legacy premade voices turned out mostly wrong (only 2 of 9 existed), fixed by replacing the pool with the account's real 21 voices, checked via `GET /v1/voices`; real synthesis, caching, and the full client pipeline all confirmed working end to end. Still open on #29: a real hand-authored voice/accent field, and tighter audio/text-reveal sync. 737 tests (added `test/voice-routes.test.js` and ElevenLabs-path coverage in `test/voice.test.js`). [PR #324](https://github.com/msdixon/secret-cabinet/pull/324)
- **2026-08-20** — [#29](https://github.com/msdixon/secret-cabinet/issues/29): fixed a bug found live-testing [PR #324](https://github.com/msdixon/secret-cabinet/pull/324) with a real ElevenLabs key — `stripForSpeech()` in `public/js/voice.js` only stripped the asterisks from an inline action aside (`*pauses thoughtfully*`), leaving its words in what got spoken, so a member would narrate their own stage directions mid-sentence. Now the whole aside is removed and whitespace collapsed. Single fix point covers both the Web Speech and ElevenLabs paths. 739 tests. [PR #329](https://github.com/msdixon/secret-cabinet/pull/329)
- **2026-08-20** — [#29](https://github.com/msdixon/secret-cabinet/issues/29) — first shippable pass on per-member voice synthesis, scoped down from the issue's `complexity: large` proposal: `public/js/voice.js` (`window.Voice`) speaks each rendered Witness speech beat via the Web Speech API, member → voice/pitch/rate assigned by a deterministic hash of `memberId` rather than a hand-authored profile (no character-file field for one yet, and `SpeechSynthesis`'s voice list is browser/OS-dependent). Off by default, toggled from a new button beside #288's speed control. ElevenLabs/paid TTS, real per-member voice/accent authoring, and audio synced tightly to the beat-by-beat reveal are named follow-ups, not attempted here — #29 stays open. [PR #319](https://github.com/msdixon/secret-cabinet/pull/319)
